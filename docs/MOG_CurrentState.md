# MOG — Current Project State

The running snapshot of where this codebase is *right now*. Updated at the end of every session that ships material changes. Future Claude reads this on session start (via `CLAUDE.md` @-import) and uses it to orient before touching anything.

If this doc and `CLAUDE.md` conflict on a specific fact, `CLAUDE.md` wins for structure / invariants; this doc wins for "what's currently in flight."

---

## Pinned focus

**KM web editor — Section B + QOL #2 + validate-first gate + per-tool mini-tours + MVS voice/chrome parity, ALL FANNED OUT (2026-06-24, later session). COMMITTED + PUSHED.** Everything that was canary-only is now live on all 9 + master (`deploy.py --redeploy`, every target OK). **(1) Validate-first gate** (`EditorShell` `mgeStartGate_` + new client-callable `editorPing` in `Editor.gs`): a stored token is validated server-side before any tool runs — session tokens live in `CacheService`, which Google flushes on a new deployment version, so the old optimistic gate ran a tool with a dead token and bricked; init now re-runs on every successful auth (`mgeRunCb_`) so a stale/expired token re-prompts and reloads cleanly. Kills the redeploy-brick + the cold deep-link brick. **(2) QOL #2 "Recently adjusted" pill** (`ManageItems`, client-side `mog_parAdjusted_<abbr>`, fires only on a par change, precedence over the historical flag) + table height now matches the open edit-form sidebar. **(3) Per-tool mini-tours** on all 5 tools (info mode, replay from `?`, auto-once/browser) + **tour gate hint** (`ⓘ Do this step to continue` + "Done with this step" — fixes the setup gating that "felt broken"). **(4) Voice rewrite** of all 5 mini-tours + the home help popup to the MVS/MPS house voice (imperative titles, action-first bodies, no "~20 seconds" meta, ES informal tú). **(5) Chrome parity:** `?` → prominent filled-accent circle next to the breadcrumb title (`.mge-crumb-help`, EN/ES stays in the band); tour/Replay buttons fixed to solid concept accent (`var(--web-accent, …)` — were navy on tool pages); help-popup header colored. **(6) Skills:** `mog-editor-web-reskin` gained **Rule 0 "port faithfully from MVS/MPS"** + a Tours & voice section + centralized-chrome correction; `mog-deploy-workflow` gained "never `--redeploy` to iterate — push-only on `/dev`." **Next: decide auto-fire vs replay-only for the 4 new mini-tours; optionally extend the natural-scroll "B" layout + subtitle to the other 4 tools; optionally fold the MVS-fidelity lessons into the global skills (this round was repo-only).** Detail: `docs/MOG_SessionHandoff_2026_06_24.md` (Later-session block).

**KM web editor — first-run SETUP WIZARD shipped + guided tours + Manage Items QOL (2026-06-24).** Two layers: **Phase A SHIPPED (committed `8aa7f6e`, pushed, fanned out all 9 + master)** — the **`?page=setup` first-run wizard** (`Setup.html` + `commitStoreSetup`, one-shot token-less guard, identity-only minus master PIN; MVS-style: language splash + Concept/FOH-BOH dropdowns + City only-free-text → auto store **name** + **code** `CONCEPT+2-city-letters+BOH/FOH` e.g. `TNYROBOH`, live concept re-theme); the **`doGet` inversion** so the **plain `/exec` opens the editor** (configured) / wizard (unconfigured), JSON health → `?page=api`, PWA unaffected (POST-only); and **ĂN (`#3C1124`) + Lei'd (`#b51579`) added to `CONCEPT_THEMES`**. **Phase B + Manage Items QOL — BUILT, CANARY ONLY (rpfrf editor / `_template` wizard), UNCOMMITTED, NOT fanned out:** shared **tour engine in `EditorShell`** (`mgeStartTour_`, `info` + `gated` modes), **home flow-tour** (Vendors→Items→Areas→Shelf to Sheet), **gated setup tour**, and Manage Items — **mini-tour** (MPS-voice), **prereq warnings** (no-vendor block / no-area tip), **natural-scroll layout + persistent title/subtitle** (unlocked `html.mge-web`), and **live per-delivery-day par preview** (`ceil(par × dayMultiplier)` — round UP to whole; mults from `getVendorTableData`). Also: **"Clear Config (reset for setup)"** menu (un-configures a store + clears concept → reuse the template to re-test the wizard). **Bug fixed:** `MIRPC` web proxy is an explicit allowlist — a missing fn (`getVendorTableData`) threw mid-init and stopped items loading. **Next: QOL #2 — client-side "Recently adjusted" Par-Review flag; then fan out Phase B (`deploy.py --redeploy`, no `--target`) + `git push`.** Open: cold deep-link brick (open `?page=items` un-authed → bricks; use the bare `/exec` home link); per-tool mini-tours for the other 4 tools. Detail: `docs/MOG_SessionHandoff_2026_06_24.md`.

**KM web editor — AUDITED, SHIPPED to all 9 + master, + TNY gold-forward + Shelf-to-Sheet rename + grouped home + guided tour (2026-06-22, later session). COMMITTED + PUSHED (`bdb44c7`).** Supersedes the "POLISH COMPLETE … NOT committed/pushed/fanned out" block below — that work is now live on every store and on `origin/main`. **(1) Code audit clean** (`appsscript-codebase-audit` + dead-code check): no dual-host dead code, no redundant init RPCs (pages render from prebaked template vars), cleanups left no residue. **Resolved the 5 Sheet-side dead-code candidates → KEEP all** (`showAdminResetSidebar`/`goToOrderEntry`/3× `toggle*Visibility`): none are menu-wired or code-called, but `showAdminResetSidebar` is the sole entry to the live Admin Reset → proves they're **dashboard-drawing-button-bound** (grep-invisible); confirm via "Assign script", don't delete. One cosmetic fix (EditorHome `:root` token dedup vs EditorShell). **(2) TNY gold-forward everywhere** (web band + dashboard banner + recap email): split `CONCEPT_THEMES` `accent` into `accent` (band fill → gold) + new **`ink`** (tiles/strip/email text → charcoal); `dashTheme_()` normalizes `ink: t.ink||t.accent` so **RP/default unchanged**; `Dashboard.gs` fills + `MOGApi.gs` email `headInk` use `ink`; web `webAccent` override removed (base is gold now). **(3) Info-notes removed** from all 5 tools + `.web-note` CSS; **breadcrumb left-aligned** (`.mge-crumb` `margin:0`). **(4) Renamed "Reorder Pick Path" → "Shelf to Sheet" / "Estante a Hoja"** everywhere shown (home tile, menu, dashboard tile, tool title/breadcrumb/help/Save, ManageItems+StorageAreas cross-refs, HowToUse); **left** code ids (`ReorderPickPath`/`page=pickpath`/`commitReorderPickPath`) + AdminReset "Pick Path DB/List" data labels. **(5) Home grouped into 3 sections** (Catalog / Order Sheet Layout / Records) + **first-run guided tour** — MVS/MPS-style **cutout-spotlight** coach-marks (4 dim panels + pulsing hole), bilingual, once/browser (`localStorage`), language from the gate, replayable from the help popup; tour engine currently in `EditorHome.html` (info-mode). **Manual carry-forward: Rebuild Home Dashboard per store** (gold banner on 2 TNY stores, Shelf-to-Sheet tile on all). **Next: build the first-time-setup wizard** (the MVS/MPS setup version MOG lacks — `?page=setup` + `commitStoreSetup` + first-run routing + language splash + gated setup tour; identity-only; promote the tour engine to EditorShell first). Detail + design + 3 open decisions: `docs/MOG_SessionHandoff_2026_06_22.md` (Later-session block).

**KM web editor — POLISH COMPLETE + shared shell + consistency sweep (2026-06-22). CANARY rpfrf `/dev` ONLY — NOT committed, NOT pushed, NOT fanned out.** All 5 tools (Manage Items, Manage Vendors, Storage Areas, Reorder Pick Path, Order History) **and the Home dashboard** now share ONE editor shell defined in **`EditorShell`**. **(1) Centralized header band** — brand **horizontal logo → location → "Master Ordering Guide"** (horizontals from `heh-brand-kit/assets/logos`, recolored white, per `data-concept`); was copy-pasted per tool (the size-drift cause). EN/ES = a **segmented control**, "?" = a **26px circle**, both **fully owned by EditorShell with `!important`** (host base styles can't leak). `--shell-width` (set per page to its content width) drives band + breadcrumb inner width so the header **aligns with content**. Each tool/Home calls `mgeMountWebChrome_()`. Reference pattern: **MPS/MVS app shells**. **(2) Modular accent** — `mgeApplyConcept_` derives all four `--web-accent*` from `THEME.accent` via pure-JS `mgeMix_` (no `color-mix` dep); removed per-tool wiring; **fixes the latent Teas'n You gold bug** (shades were RP-teal-hardcoded). **(3) Shared `.web-note` info-note** — one-line bilingual accent callout, now on all 5 tools. **(4) Consistency sweep** (`appsscript-ui-consistency-audit`, web layer only) — web token set (`--web-accent*`/`--shadow`/`--font`/`--r`/`--r-sm`/`--muted`/`--faint`) **defined once in EditorShell `:root`**, 5 per-tool copies removed; **fixed `ManageItems` referencing undefined `--r`/`--faint`**; tokenized stray radii; normalized input sizing. **Everything `body.mge-web`-scoped → Sheet dialogs byte-identical.** Per-tool re-skins (Vendors 920px, Pick Path 780px Treatment-B headers, Order History 1080px pill tabs + Recent card grid, read-only) all mockup-approved first. **Next: code audit (dead code + inefficiencies) — `appsscript-codebase-audit` + `mog-pwa-audit`; then guided tour, fan out (`deploy.py --redeploy`, no `--target`), `git push`.** Detail: `docs/MOG_SessionHandoff_2026_06_22.md`.

**KM web editor — Phase 1 shipped (2026-06-21). CANARY rpfrf ONLY — NOT pushed to GitHub, NOT fanned out.** The parked KM-editor feature is built: a **computer-only, PIN-gated web app** served from the existing `/exec` URL via **`doGet?page=…`** (separate door from the PWA — `doPost` untouched, phones confirmed unaffected). **5 live tools**: Manage Items, Manage Vendors, Storage Areas, Reorder Pick Path, Order History (read-only). **Admin Reset deliberately Sheet-only** (destructive; not exposed to KMs). 3 commits (`4b5c353`, `fe47a12`, `1a3a799`), all `deploy.py --redeploy --target rpfrf`. **Architecture:** (1) **dual-host modals** — each existing modal (`ManageItems`/`ManageVendors`/`StorageAreas`/`ReorderPickPath`/`OrderHistory`.html) serves BOTH the Sheet dialog and the web page via a `MOG_WEB` flag (`webBootJson`); Sheet launchers inject `web:false` so the dialog is unchanged → **parity by construction**. (2) **`Editor.gs`** (new): `renderEditorHome_` + per-tool `render<Tool>Web_`; token-backed PIN (`editorAuth` reuses `checkPin_`+lockout); **one allowlisted dispatcher `webedit_call(token,fn,args)`** every web RPC routes through (the `webeditDispatch_` switch IS the allowlist). (3) **`EditorShell.html`** (new, shared): concept-themed login (real brand logo + `dashTheme_()` accent), device gate, EN/ES, breadcrumb. (4) **`EditorHome.html`** (new): MVS-faithful dashboard (Inter, 880px, fixed 3-col tiles) + help CTA. Client RPC shim `MIRPC()` per modal (Sheet→`google.script.run`, web→token proxy). **Gotchas:** `google.script.run` can't call `_`-suffixed fns; a blank page = `hidden`-attr-vs-CSS-`display` override (fixed via `.mge-overlay[hidden]` in EditorShell); token shared cross-page via `localStorage`; `target=_top`+injected `BASE_URL` for links. **Candidate #1 DONE.** **Next: POLISH each tool's per-page look (now unboxed from the Sheet modal), then the guided tour, then fan out (`deploy.py --redeploy`, no `--target`).** Detail: `docs/MOG_SessionHandoff_2026_06_21.md`.

**God-object split (#4) + new-day-detection dedup (#5) (2026-06-19, later session).** 3 commits, all deployed to all 9 + master via `deploy.py --redeploy` (canary rpfrf smoke-tested: menu, all modals, dashboard rebuild, reset, order history). **(A) Split `OrderGuideScript.gs` (5,497 lines) → `Core.gs` + 6 domain modules** (`38382db`): `Vendors.gs` / `Items.gs` / `PickPath.gs` / `ResetLog.gs` / `History.gs` / `Dashboard.gs`; `Core.gs` holds all global constants + generic helpers + menu/`onOpen`/`onEdit`/triggers + order-cycle date helpers. **Pure code-motion** done via a deterministic slicer that verified byte-exact reassembly + 144/144 declaration parity before writing; `node --check` clean on all 7, zero duplicate symbols across all 8 `.gs`. Load-order is a non-issue because **all top-level constants stay co-located in Core** (the only top-level cross-refs are within the constants block) — no `filePushOrder`/`deploy.py` change. **(B) Dedup new-day detection** (`a771767`): two canonical `Core.gs` helpers — `getActiveOrderDate_()` → `{date,dateStr,dayOfWeek}` and `getResetStaleness_()` → `{today,lastReset,isStale}`; 4 sites rewired (`api_getResetStatus_`, `dailyResetOnOpen_`, `getLogOrderDate_`, MOGApi's old copy removed); **TZ standardized on `getSpreadsheetTimeZone()`** (all stores US/Eastern, no-op in practice). **(C) Doc/skill/comment ref updates** (`15ec141`): repointed `CLAUDE.md`/`README`/`apps-script/README` inventories, 2 pattern skills, and 8 inline `.gs` comment pointers off the deleted file. **Candidate #1 and #5 are now DONE.** **Next: the PARKED KM-editor feature** (computer-only + PIN-gated; progressive Option A — `doGet?page=…`). Detail: `docs/MOG_SessionHandoff_2026_06_19.md` (Later-session block).

**Reset-fix + brand logos + codebase audit & cleanup (2026-06-19).** Three bodies of work, 4 commits, all pushed; backend cleanup fanned out to all 9 + master (canary rpfrf). **(A) PWA auto-reset stranding fixed** (`cdc13f2`, CACHE v16→v17): `commitReset` now runs under a 45s timeout (new optional `api()` `timeoutMs` arg; default stays 15s) and `runStaleReset_` **re-polls `getResetStatus` on any failure** — Apps Script finishes the reset server-side even when the client aborts, so the re-check enters the home screen via new `finishResetAndEnterApp_()` instead of stranding KMs on the reset screen. **(B) Brand SVG logos on PIN/splash** (`e805147`+`dab9b51`, CACHE v17→v19): single-color Canva lockups recolored via `currentColor`, swapped in for the clipboard square per `[data-theme]`. Live: RP `rp-stacked` (basil `--teal-dark`), TNY `tny-horizontal` (charcoal `--text`). **Wired dormant ahead of launch:** ĂN `an-stacked` (house green `--teal`), Lei'd `leid-horizontal` (magenta) — their theme rules only fire when a store ships `data-theme="an"/"leid"` (none do yet). **(C) Two-layer codebase audit + quick-win cleanup** (`c9023ab`, CACHE v19→v20): audit verdict **clean** (no perf debt / drift / fan-out / shell issues). Removed 7 dead backend fns (181 lines; `OrderGuideScript.gs` 5669→5497, `MOGApi.gs` 2149→2141 — `commitUpdateVendorMults`/`commitUpdateVendorCutoff` superseded, empty `menuHeader_`, orphaned `reestablishVendorTemplateMenu_`, `hasLogEntryForDate_`, `getItemsByVendor`, `generateReference_`; deployed `deploy.py --redeploy` all 9 + master). PWA: added missing `recap` key to `T.titles` (blank "Full order list" topbar), `back-count-btn`→`reopenCountFromContext_()` (offline revert), dropped dead `result.cancelled` branches + a spent migration `console.log`. **Carry-forward:** VERIFY-then-delete `showAdminResetSidebar`/`goToOrderEntry`/3× `toggle*Visibility` (may be dashboard-button-assigned); then **#4 god-object split + #5 new-day dedup** (walkthrough first); then the **PARKED KM-editor feature** (locked: computer-only + PIN-gated; recommended progressive Option A — serve modals via `doGet?page=…`). Detail: `docs/MOG_SessionHandoff_2026_06_19.md`.

**Skills consolidation slice (2026-06-10) — no app code changed, nothing to deploy.** `handoff_facts.py` and the i18n parity checker are now logic-free wrappers over promoted/canonical globals; `mog-session-handoff` now waits for an explicit wrap-up signal (was auto-trigger); RPC + caching patterns absorbed into globals (`appsscript-rpc-bootstrap`, `appsscript-caching`) with defer preambles here. Detail: `docs/MOG_SessionHandoff_2026_06_10.md`.

**Vendor Import feature shipped (2026-06-08).** All to all 9 + master via `python deploy.py` (push-only), canary rprfo smoke-tested first. New "⬆ Import Vendor" panel in `ManageVendors.html` (between Add and Remove): KM downloads a 2-column CSV template (Item Name, Pack Size — generated client-side as a Blob, no server roundtrip/scope), fills it on a computer, picks delivery days + optional cutoff in the modal, uploads the CSV. New server fn **`commitImportVendor({name, mults, cutoff, csvText})`** in `OrderGuideScript.gs` (after `commitAddVendor`) parses via `Utilities.parseCsv` (skips blank/`#`-comment/header rows, dedupes by name), reuses `commitAddVendor` for the vendor (SETUP block + VENDOR_TEMPLATE clone), then **bulk-inserts items into MASTER_ITEMS in batched `setValues` calls** (not per-item) with **par BLANK and no storage-area assignment** — so items exist but stay OFF the vendor's ordering tab until assigned an area in Manage Items (matches the "skeleton catalog" ask). Items default ACTIVE + USE_MULT true, eligible-vendors = just the imported vendor. Delivery → mults via existing `computeMultsFromDelivery`. File read client-side (FileReader→text, desktop-only by design). 6 EN/ES keys added (parity 34/34 PASS); `.gs` + modal JS parse clean. **Bound-sidebar change → `python deploy.py` (push-only, NO `--redeploy`). SHIPPED: canary rprfo smoke-tested, then fanned out to all 9 + master.**

**Recap-email rebrand (Phase 2) shipped + modal sweep fanned out (2026-06-04, later session).** All to all 9 + master, canary rprfo smoke-tested. **(A) Modal design-system sweep fanned out** (`python deploy.py`, push-only) — closed the 2026-06-04 canary gate; the shared `Styles.html` tokens + two-tier sizing + `[?]` fixes are now live everywhere. **(B) Daily recap email rebranded** (`MOGApi.gs sendRecapEmail_`, `--redeploy`): old "warm" palette → **per-concept theming via the existing `dashTheme_()`** (reads `MOG_CONCEPT`; RP teal-dark `#2d8c6b`+white, TNY charcoal `#1a1a1a`+gold `#D4A574`, unset→navy — same palette as the Sheet dashboard, callable from `MOGApi.gs` via shared global scope, `typeof`-guarded). **Header band**: location name **bold on top**, "Suggested daily order" below, faint `date · vendors · items` meta. **Careful "suggested" wording** (caption + `Qty`→"Suggested order" column). **Each line reads `Item Name × 3 (Pack)`** — name in body ink, the **× qty bold in the concept accent**, pack muted; replaced the old 4-col grid. **On Hand isolated** in a faint right column (12px, `#aab0b6`). Email-safe: literal hexes, single-cell `<table>` band. **(C) NEW editor-only `test_recapEmailToSelf()`** — sends a preview only to whoever runs it (`Session.getActiveUser()`), bypassing the recipient list / dedupe flag / On-Hand clear; not menu-wired. **No new OAuth scopes** (`userinfo.email`/`script.send_mail`/`spreadsheets` already declared) → fan-out's `--redeploy` re-prompts nobody; **do NOT add scopes to `appsscript.json`**. **Optional next:** per-concept hub brand SVGs, or Batch D (brand fonts / concept-aware modal theming — easy now via the `:root` token layer).

**Backend cleanup + per-modal [?] help shipped (2026-06-01, cleanup session).** All to all 9 + master, canary rprfo smoke-tested on both deploys. **(A) Backend cleanup** (`deploy.py --redeploy`), grounded in a read-only `appsscript-codebase-audit`: deleted dead `migrateVendorTabs` + `brandAndStripVendorTab_`, `getOrderSummary`, `getOrderHistoryVendorList` (~230 lines; `OrderGuideScript.gs` 5783→~5550); decluttered the Mobile API submenu — removed **Migrate Item Vendors** (spent backfill), **Audit Vendor Tab Structure**, **Re-establish Vendor Template** menu entries (**functions kept, editor-runnable**); trimmed dead `dayOfWeek/vendorMults` args off `vendorOnHandSnapshot_`; fixed 3 stale comments. `purgeInactiveFromPickPath` **deliberately kept** (intentional editor-run tool, not dead). **(B) Per-modal [?] help** (push-only): a round [?] button in each modal header opens an inline bilingual help overlay (✕/tap-outside/Esc), identical CSS/JS block across **6 modals** (StorageAreas canary first, then ManageVendors/ManageItems/AdminReset/ReorderPickPath/OrderHistory), `currentColor`-adaptive, paired `.en/.es` spans (parity untouched). **(C) How-To-Use de-staled** (`HowToUse.html`): Items Edit-tab→row+Edit This Item, Areas drag→▲▼+Save Areas, Order History 📋Copy→🖨Print, all Pick Path drag→▲▼/⇆/+. **Deferred (next session):** the `OrderGuideScript.gs` 7-file god-object split (walkthrough first) + new-day-detection consolidation (~5 sites, rides along); optional guide additions (PWA pointer, multi-vendor switch, Recalibrate, recap-email). **Optional next:** the split, per-concept hub brand SVGs, or Batch D.

**Modal design-system unification — FANNED OUT (2026-06-04, later session closed the gate).** A cohesion sweep across all 9 Apps Script modals, grounded in `appsscript-ui-consistency-audit`. **NEW `apps-script/Styles.html`** is now the single source of truth: a `:root` token block + the universal chrome (`*` reset, `.lang-*`, the `[?]` `.help-*` overlay) that was copy-pasted in every modal (~200 dup lines removed), pulled in via `<?!= include('Styles'); ?>`. New `include()` helper in `OrderGuideScript.gs`; the 3 flat modals (HowToUse/AdminReset/OrderHistory) converted `createHtmlOutputFromFile`→`createTemplateFromFile().evaluate()`. Every brand/semantic color tokenized (`--brand` navy header everywhere, `--brand-green` interior headers, `--accent`, `--success`, `--danger`, `--warn`); neutral greys deliberately left raw. **AdminReset red `#7a1a1a` split**: header→`--brand` (navy, uniform), destructive affordances→`--danger`. **Two-tier sizing** via `MODAL_SM` (720×680) / `MODAL_LG` (1400×900) constants — SMALL: Vendors/Storage/AdminReset/PickPath/Recalibrate/Cadence; LARGE: Items/OrderHistory(now matches Items)/HowToUse. Fixed `[?]` off-center on Items/History (`.lang-group{align-items:center}` in Styles) + invisible `[?]` on PickPath (`.lang-bar{color:#fff}`). OrderHistory readability bumped (base 14/table 13). **GATE CLOSED — rprfo smoke-tested, then `python deploy.py` (push-only) fanned out to all 9 + master (2026-06-04 later session).** The two size judgment calls (720×680 / 1400×900, HowToUse tier) are live and still nudgeable (two constant pairs).

**Ordering-flow breadcrumb + vendor-name topbar shipped (2026-06-01, later session).** Two PWA commits, both pushed to `main` (GitHub Pages, CACHE **v14→v15→v16**). After confirming the History breadcrumb works live on rprfo (gate below now closed), extended the same drill-down breadcrumb to the ordering flow: **count** shows `Today's orders › <vendor>`, **review** shows `Today's orders › <vendor> › Review order`. Ancestor crumbs tappable (root → today, vendor → count); the count root crumb routes through `goBack` so the mid-count save/discard guard still fires; **the review back button now steps review→count** (was review→today) to match the crumb. **Key correctness catch:** review→count re-shows from the in-memory count context via new `reopenCountFromContext_()` — `openReview`'s flush does no-refetch and *drops the in-memory vendor cache*, so routing through `openCount` would have re-fetched a stale localStorage snapshot and reverted the just-entered counts. New `setCountBreadcrumb_` (shared, no drift), `showView` keep-list widened to `count`/`review`. Second commit (`74529c0`): per Sebastian's live review, the count/review **topbar shows the vendor name again** (clarity) while the breadcrumb stays underneath — `titles.count`/`titles.review` keys remain as the empty-vendor fallback; resolves the "Review order shows twice" concern. Commits `3631927` + `74529c0`. **Optional next:** per-concept hub brand SVGs, or Batch D.

**Housekeeping batch + PWA History breadcrumb shipped (2026-06-01).** Two commits, both pushed. **(A) Backend housekeeping** (`4d5a05e`, all 9 + master via `deploy.py --redeploy`, canary rprfo smoke-tested): `commitUpsertItem` now returns `areaError` on a failed inline area assignment (both ManageItems partial-success toasts append the reason — item still saves `ok:true`, we just stopped swallowing *why* the area step failed); retired dead `api_getHistory_` (the "recap uses it" comment was false — recap uses `buildRecapSections_`; PWA migrated off `getHistory` 2026-05-28) + its dispatch case + `test_getHistory`; reconciled the **Rhino-ES5 invariant** in MOG docs (modals are browser-side/ES6-safe, `.gs` is V8 — invariant #4 + pitfall #5 corrected; **global `rhino-safe-html` skill left as optional style**, scoped by Sebastian); **purged the parallelize-`deploy.py` candidate** (decision record now in architecture notes + a "Dropped (do not re-add)" note — do NOT re-propose it). **(B) PWA History breadcrumb** (`a0ca5b6`, GitHub Pages, CACHE **v13→v14**): a drill-down breadcrumb under the topbar on the two Order-History sub-views — `Order history › <date> › <vendor>`, ancestor crumbs tappable (jump to that level), current level static, hidden on the dates root. `setBreadcrumb_`/`goToHistoryRoot_` in `template/index.html`; redundant in-view date/vendor headers + the topbar date override removed (the clean dedup variant). Brand-neutral gray chrome (per Batch A's "chrome stays constant, brand re-themes"). **GATE CLOSED — History breadcrumb confirmed working live on rprfo (2026-06-01 later session).** **Optional next:** per-concept hub brand SVGs, or Batch D.

**Hub 3-level nav + per-concept tile theming shipped (2026-05-31, later session).** The hub is now a **Concept → Site → Area** picker (was Concept → flat location list): RP "3 locations" → Founders/Rosslyn/Tysons → BOH/FOH; single-site (TNY) and single-area sites auto-collapse. Site/area are **derived from the `location` string in `index.html`** (Option A — `parseLocation_`/`buildSiteGroups_`; deliberately NO `stores.json` schema change, so `build.py` + `mog-add-store` are untouched). Concept tiles now theme per concept (RP basil `#53D3A5`, TNY gold `#D4A574` via new `tint`/`tintDark`/`tintLight` in `CONCEPT_VISUALS`); the active concept's tint carries into the site/area sub-screens via `--concept-*` CSS vars on `#app`, clearing to HEH-red on the picker. **TNY display name → curly apostrophe** `Teas’n You` (U+2019), synced across the 3 coupled spots (`stores.json` + hub `CONCEPT_VISUALS` key + `build.py` `CONCEPT_TO_THEME` key). `audit_modals.py` rebuilt to auto-discover save-capable modals (now covers `RecalibrateVendor.html`). Latent fix: `toggleHubLang_` now re-renders the picker (the "N locations" meta is built text, not bilingual spans). **HUB-ONLY + tooling change** — verified in a local preview, hub `sw.js` CACHE **v6→v7**, the 8 store dirs came out byte-identical (no clasp deploy). Deploys via `git push`. **Optional next:** wire real per-concept brand SVGs into `CONCEPT_VISUALS` (still generic Tabler icons + tints), or Batch D.

**Visual-consistency sweep shipped (2026-05-31).** Audited Hub + PWA + Modals against 3 yardsticks (internal cross-surface, Canva brand-kit, UI polish). Three commits live + deployed: **Batch A** (`41f2e8b`) — PWA teal-tinted borders/toasts now re-theme per concept via a new `--teal-border` token (CACHE v13); principle: brand/primary re-themes, red/amber warnings stay constant. **Batch B** (`3b48218`) — HowToUse header→navy, modal page bg unified `#f4f5f7`, ReorderPickPath glyph `×`→`✕`, hub meta theme-color→red (hub CACHE v6); ManageItems navy base button LEFT navy (intentional). **Manage Items** (`7210e0d`) — bigger table legend + gray "✓ No change"/"No data yet" Par Review pills. **Brand fix (outside repo):** Roll Play primary corrected to **basil green `#53D3A5`** in `rp-design-tokens.md` (orange→accent); PWA already used `#53D3A5` so app + brand doc agree. Nothing broken/half-finished. **Optional next:** per-concept HUB theming (hub still HEH-red for all concepts; `CONCEPT_VISUALS`/`conceptIconHtml_` already accept per-concept brand SVGs), and "Batch D" (brand fonts / SVG concept marks / concept-aware modal theming).

**Vendor-multiplier fragility RESOLVED + verified (2026-05-29, evening).** The hidden `VENDOR_TEMPLATE`'s `H2` multiplier formula was stale (dead `ORDER_ENTRY!$B$4`/`$D$2` refs) on 6 of 7 templates incl. the master, so Add-Vendor clones were born with multiplier=0 (nothing orderable). Root cause: `updateVendorTabHeader2Formulas_` looped the live vendor list only and never touched the hidden template. Fix shipped to all 9 + master (push only): `updateVendorTabHeader2Formulas_` now also repairs the template; `vendorTabH2Formula_()` is the canonical single source; new non-destructive menu **Sync Vendor Multiplier Formulas**; fail-safe Add Vendor (dropped the `ss.getSheets()[3]` fallback); `reestablishVendorTemplate_` + menu recovers templateless stores. **VERIFIED clean across all 9 + master from fresh xlsx exports: template present everywhere, all H2 = AD2/AE3, zero stale tabs; tnytf Setup re-run.** Cosmetic #4 (branding / dead-zone strip) DROPPED; `migrateVendorTabs`/`brandAndStripVendorTab_` shelved in-file (unwired from menu, safe to delete). Sheets proven healthy (tabs are identical clones; the `M3`/`I4`/`K4` audit "variants" were just `__xludf.DUMMYFUNCTION` cached-value tails). The (A)/(B)/(C) items and "#4 build is next session" below are SUPERSEDED, kept for history.

_Earlier the same day (later session) — superseded context:_ Two shipped + #4 scoped. All deployed to 9 via clasp.

- **(A) ManageVendors multiplier→delivery clarity** (`ManageVendors.html`, bound-sidebar) — per-cell "→ <delivery day>" arrows under each active multiplier (Add + Edit) + a worked-example callout at the top of the Edit form (EN/ES). Latent fix: `setLang` never re-rendered the vendor cards, so the EN/ES toggle left card chrome in the build-time language → added `rerenderVendorCards_()`. i18n parity 26/26.
- **(B) Dashboard per-store branding** (`MOGApi.gs` + `OrderGuideScript.gs`, `--redeploy`) — `buildHomeBanner_` was hardcoded `"ORDERING GUIDE · ROSSLYN"` (every rebuild stamped ROSSLYN); now reads `MOG_LOCATION_NAME`. New `MOG_CONCEPT` property drives the accent via `CONCEPT_THEMES` + memoized `dashTheme_()` (RP teal-dark `#2d8c6b`/white, TNY charcoal `#1a1a1a`/gold `#D4A574`; unset→navy). Set via `setStoreConcept()` + extended `setupMobileApi()`. rprfo + tnyt verified; **Sebastian to set concept + rebuild dashboard on the other 6** (RP: rpr/rpt/rptfo/rpfr/rpfrf; TNY: tnytf).
- **(C) #4 (vendor-tab redesign) scoped + de-risked.** Read-only `auditVendorTabStructure()` shipped to all 9 (Mobile API menu). **Finding: VENDOR_TEMPLATE was DELETED on rprfo** — add-vendor *copies* it (doesn't recreate) and falls back to `ss.getSheets()[3]` when missing (fragile; ⚠ don't Add Vendor on a template-missing store). The uploaded xlsx is STALE vs live — trust the **live** VENDOR_TEMPLATE. Order block (I–L) is human-facing only (recap/log build from A/B/E/F/M); N–T are a dead zone (Q–T = unreferenced duplicate of A–D).

**#4 build is next session:** (A) re-establish a canonical hidden VENDOR_TEMPLATE per store; (B) harden add-vendor to fail-safe; (C) dead-column strip (clear `Q3:T3`, hide `N:T`) + concept header branding in template + all tabs; (D) `migrateVendorTabs()` — idempotent/defensive/On-Hand-safe. Gate: run the audit on all 9 + a `mog-sheet-formula-verify` pass. Canary rprfo.

**Parked:** #2 assign-after-reset bug (model confirmed: live spill keyed on M, On Hand E position-pinned; couldn't reproduce — watch for recurrence; latent `commitUpsertItem` silent-swallow of area-assign failures worth fixing). PWA-slower-than-Sheet = architectural (`/exec` cold-start + re-auth vs warm in-Sheet), no action.

### Prior session pinned focus (carried for context)

**Earlier 2026-05-29:** Vendor Cadence Audit sidebar (`9b4c027`) + PWA decimal On Hand (`daf354f`, CACHE_VERSION v11→v12). The ManageVendors Edit-form **"Advanced" disclosure** stays deferred (gated on cadence-audit cleanup): Gate 1 = hide raw mults grid behind "Advanced" toggle; Gate 2 = **NO** lead-day picker; Gate 3 = **NO** schema migration.

**2026-05-28:** Five wins (StorageAreas draft mode `ed49a62`, PWA transition-floor cut `6f9197f`, PWA fetch timeout `ba30b8e`, chunked Order History `47a1966`, Recalibrate Vendor Pars tool) — all live; `CACHE_VERSION` v8→v11. See the Recent-changes table below + handoffs for detail. *Recalibrate Vendor still to be run per-vendor at rpr at Sebastian's pace.*

### Next-session candidates (impact-ranked)

1. **KM web editor — mini-tour fire policy + "B" layout for the other tools** (MEDIUM, next-session focus). (a) **Auto-fire vs replay-only:** the 4 new per-tool mini-tours currently auto-fire once/browser (matching Manage Items); with the home flow-tour + 5 per-tool tours that may be a lot of coaching — decide whether to switch the per-tool ones to replay-only (drop the `maybeAutoStart…` calls). (b) **Natural-scroll "B" layout + persistent subtitle** only exists on Manage Items; convert the other 4 tools (un-lock `html.mge-web`, cap the scroller, sticky detail) when adding their subtitles. (c) Optional: fold the MVS/MPS-fidelity lessons into the GLOBAL skills (this round was repo-only — `appsscript-guided-tour-help` / `appsscript-phrasing-glossary` / `appsscript-first-run-setup` / `architectural-walkthrough`). *(DONE 2026-06-24 later: QOL #2 pill, validate-first gate (kills the cold deep-link brick), per-tool mini-tours on all 5, MVS voice + `?`/EN-ES chrome parity — all fanned out + committed.)*
2. **ManageVendors Edit-form "Advanced" disclosure** (MEDIUM). Hide the raw mults grid behind an Advanced toggle so the delivery-day picker is THE input. **Gated on Sebastian running Vendor Cadence Audit across all 9 + recalibrating flagged vendors** — silent-rewrite-on-save risk. Walkthrough done.
2. **Real per-concept brand SVGs on the hub** (OPTIONAL). Per-concept *color tiles* shipped 2026-05-31 (basil/gold tints), but the concept icons are still generic Tabler glyphs (`soup`/`cup`). `CONCEPT_VISUALS` already accepts an `svg:` field that `conceptIconHtml_` prefers over `icon:` — drop real brand marks in to finish. Hub `git push` + cache bump.
3. **Batch D — brand expression** (OPTIONAL, strategic, from 2026-05-31 sweep). Brand fonts (Brother 1816 / Avenir for RP, Campaign Serif / Filson Pro for TNY — PWA currently Archivo Black + Inter, modals Arial); brand SVG concept marks on the hub (see #2); concept-aware modal theming (modals are a fixed navy/green across all 9).

> **Dropped (do not re-add):** *Parallelize `deploy.py`.* Considered across several sessions and deliberately abandoned — ~30s serial push isn't hurting, and `ThreadPoolExecutor` + per-target temp `.clasp.json` dirs add real complexity/rate-limit risk for no operational gain. See architecture notes.

*(Done 2026-06-19 later session: **#4 god-object split** — `OrderGuideScript.gs` (5,497 lines) → `Core.gs` + Vendors/Items/PickPath/ResetLog/History/Dashboard, pure motion via a byte-exact-verified deterministic slicer (`38382db`); **#5 new-day dedup** — `getActiveOrderDate_`/`getResetStaleness_` canonical in Core, 4 sites rewired, TZ standardized on `getSpreadsheetTimeZone()` (`a771767`); doc/skill/comment refs updated off the deleted file (`15ec141`). All deployed all 9 + master via `deploy.py --redeploy`, canary rpfrf smoke-tested. Candidate #1 + #5 retired. See Pinned focus + Recent-changes row.)*
*(Done 2026-06-01 cleanup session: backend cleanup grounded in `appsscript-codebase-audit` — deleted 3 dead fns (`migrateVendorTabs`+`brandAndStripVendorTab_`, `getOrderSummary`, `getOrderHistoryVendorList`, ~230 lines), decluttered Mobile API submenu (3 entries removed, functions kept), trimmed `vendorOnHandSnapshot_` args, fixed 3 stale comments (`deploy.py --redeploy`, all 9 + master); per-modal **[?] help button** + inline bilingual overlay swept across 6 modals; **HowToUse de-staled** (Items/Areas/OrderHistory/PickPath). `purgeInactiveFromPickPath` kept (intentional editor-run tool). Canary rprfo on both deploys. Deferred: `OrderGuideScript.gs` 7-file split (candidate #1) + new-day-detection consolidation. See Pinned focus + Recent-changes row.)*
*(Done 2026-06-01 later session: ordering-flow drill-down breadcrumb (count + review, mirrors History) + review→count via in-memory re-show (`reopenCountFromContext_`, avoids the stale-cache revert) + back-button review→count, then vendor name restored to the count/review topbar per Sebastian. Two PWA commits `3631927`+`74529c0`, CACHE v16, GitHub Pages, no clasp. History breadcrumb gate closed (confirmed live on rprfo). See Pinned focus + Recent-changes row.)*
*(Done 2026-05-31 (housekeeping batch): `commitUpsertItem` silent-swallow fix (server now returns `areaError`; both ManageItems partial-success toasts show the reason), retired `api_getHistory_` (dead — PWA migrated off it 2026-05-28, recap never used it; removed fn + dispatch case + `test_getHistory`), reconciled the Rhino-ES5 invariant (modals are browser-side/ES6-safe; invariant #4 + pitfall #5 corrected, global `rhino-safe-html` left as optional style), and dropped the parallelize-`deploy.py` candidate. Deploy via `python deploy.py --redeploy`, canary rprfo.)*
*(Done 2026-05-31 later session: hub 3-level nav (Concept → Site → Area) + per-concept tile theming + TNY curly apostrophe + `audit_modals.py` auto-discovery widening. Hub-only, CACHE v7, no clasp deploy. See Pinned focus + Recent-changes row.)*
*(Done 2026-05-31: visual-consistency sweep — Batch A (PWA `--teal-border` re-theming, `41f2e8b`), Batch B (modal/hub chrome unification, `3b48218`), Manage Items legend + Par Review pills (`7210e0d`), all live; RP primary corrected to basil `#53D3A5` in the brand-kit skill. See Pinned focus + Recent-changes row.)*
*(Done earlier 2026-05-29: ManageVendors multiplier clarity + Dashboard branding shipped to all 9; #4 vendor-template H2 fix verified; #2 parked.)*

---

## Standing invariants (carry forward)

Duplicated from `CLAUDE.md` so this doc reads standalone:

1. Never edit generated `<slug>/` dirs — overwritten by `build.py`.
2. Never edit code in the Apps Script editor — overwritten by `deploy.py`.
3. `.gs` files are identical across all 9 deploy targets; per-store config lives in spreadsheet data.
4. Apps Script HTML modals render in the browser (IFRAME sandbox), not Rhino — modern ES6 is safe and already in use; `.gs` runs on V8. (The old "Rhino/ES5" framing was inaccurate; the global `rhino-safe-html` skill is now optional style, not a hard constraint.)
5. `template/index.html` placeholders (`__MOG_API_URL__`, `__MOG_THEME__`, `__MOG_APPLE_TOUCH_ICON__`) appear exactly once each; never replace by hand.
6. `STORE_REGISTRY` line in root `index.html` is build-injected; edit `stores.json` instead.
7. Bump `CACHE_VERSION` when shipping shell changes so KMs' phones evict stale caches.
8. Slugs in `stores.json` are immutable once published (bookmark/home-screen breakage).

---

## Deploy targets (apps-script/.clasp-targets.json)

9 targets total. Code is identical across all of them; `appsscript.json` manifest is unified to rpr's (which explicitly declares OAuth scopes).

Each non-template target has two identifiers committed to git: `scriptId` (for `clasp push` — source) and `deploymentId` (for `clasp deploy --deploymentId` — bumping the web-app `/exec` URL the PWA hits). `_template` has no `deploymentId` because it isn't published as a web app.

| Slug | Store | Notes |
|---|---|---|
| `_template` | Master template | Copied to seed new stores. Push keeps it current. No web-app deployment. |
| `rpr` | Roll Play - Rosslyn BOH | Canary target — first to receive new deploys for smoke testing. |
| `rprfo` | Roll Play - Rosslyn FOH | |
| `rpt` | Roll Play - Tysons BOH | |
| `rptfo` | Roll Play - Tysons FOH | Added 2026-05-24. |
| `rpfr` | Roll Play - Founders BOH | |
| `rpfrf` | Roll Play - Founders FOH | Added 2026-05-24. |
| `tnyt` | Teas'n You - Tysons BOH | |
| `tnytf` | Teas'n You - Tysons FOH | |

**Deploy commands from repo root:**

| Goal | Command |
|---|---|
| Push (bound-sidebar-only change) | `python deploy.py` |
| Push + redeploy web-app URL (MOGApi.gs / any `api_*` change) | `python deploy.py --redeploy` |
| Push + redeploy with a tag | `python deploy.py --redeploy --description "<msg>"` |
| Single target | `python deploy.py --target <slug>` (combine with `--redeploy` if needed) |
| Dry run | `python deploy.py --dry-run` (combine with `--redeploy` to preview the redeploy step too) |
| Discover deploymentIds (one-time per fresh checkout if `.clasp-targets.json` has `FILL_ME_IN`) | `python deploy.py --discover` |

**Push vs redeploy — when to use which:**
- *Bound sidebars* (ManageVendors, ManageItems, OrderHistory, etc.) read HEAD inside the Sheet. Push is enough.
- *PWA* hits each Sheet's `/exec` URL, which serves a versioned snapshot. MOGApi.gs changes need `--redeploy` or the PWA stays on old code.
- When unsure: `--redeploy`. Extra cost is ~3s per target.

---

## Live stores (stores.json)

8 entries — the master template isn't published to the PWA hub (it has no deployment URL because it isn't deployed as a web app).

| Slug | URL | Concept | Location |
|---|---|---|---|
| `rpr` | `sebheh.github.io/mog-mobile/rpr/` | Roll Play | Rosslyn BOH |
| `rprfo` | `.../rprfo/` | Roll Play | Rosslyn FOH |
| `rpt` | `.../rpt/` | Roll Play | Tysons BOH |
| `rptfo` | `.../rptfo/` | Roll Play | Tysons FOH |
| `rpfr` | `.../rpfr/` | Roll Play | Founders BOH |
| `rpfrf` | `.../rpfrf/` | Roll Play | Founders FOH |
| `tnyt` | `.../tnyt/` | Teas'n You | Tysons BOH |
| `tnytf` | `.../tnytf/` | Teas'n You | Tysons FOH |

Hub URL: `https://sebheh.github.io/mog-mobile/` — concept picker with auto-redirect on return visits.

---

## Open issues / risks

- **Master-Ordering-Guide repo still exists** at `github.com/SebHEH/Master-Ordering-Guide` and locally at `C:\Users\RAD-SEB\Documents\GitHub\Master-Ordering-Guide\`. Scheduled for deletion ~2026-05-31. Risk: someone (or future-Claude) opens a session from the old dir and edits stale files, splitting the source-of-truth again. Mitigation: when Sebastian opens a Claude Code session, confirm `cwd` is `mog-mobile`, not `Master-Ordering-Guide`.
- **No CI** — the only verification of a deploy is Sebastian opening the live URL. Workable for a 1-operator setup but means PRs from anyone else would have no safety net. Not urgent.
- **`python3` shim hijack on Windows** — the Microsoft Store stub intercepts `python3`. Use `python` or `py` instead. `build.py`'s shebang `#!/usr/bin/env python3` only matters on Unix.
- **PowerShell PATH staleness post-install.** After a `winget install`, in-session shells don't pick up new tools until PATH is refreshed from registry. The convention in `CLAUDE.md` covers this.

---

## Architecture notes worth remembering

- **Two separate `CACHE_VERSION` constants** — one in `sw.js` (hub) and one in `template/sw.js` (per-store). They move independently. Bump only the one whose shell actually changed.
- **`build.py` is idempotent and zero-arg.** Running it without changes is a no-op. `--dry-run` previews without writing.
- **`deploy.py` writes a temporary `apps-script/.clasp.json` per target, runs `clasp push -f` from inside `apps-script/`, then cleans up.** `.clasp.json` is gitignored. Don't try to maintain a permanent one — the deploy script owns that file. Discovery mode (`--discover`) uses the same temp-file pattern.
- **Source push and web-app redeploy are separate phases.** `clasp push` updates the script project (which bound sidebars read from HEAD). `clasp deploy --deploymentId <id>` bumps the version that the `/exec` URL the PWA hits actually serves. `deploy.py --redeploy` does both; the default does just push.
- **`deploy.py` is Python stdlib-only (no `pip install` step), zero-dep, cross-platform.** Replaced the earlier PowerShell pair (`deploy.ps1` + `discover-deployments.ps1`) on 2026-05-26 to match `build.py`'s placement and pattern and to drop the Windows-only ExecutionPolicy requirement.
- **`deploy.py` is intentionally serial — do NOT propose parallelizing it.** A `ThreadPoolExecutor` + per-target temp `.clasp.json` dirs would shave the ~30s all-targets push to ~5s, but it was repeatedly raised and deliberately dropped (2026-05-31): 30s isn't hurting, and concurrent `clasp push` invites OAuth/API rate-limit flakiness plus working-dir contention for no operational gain. Resolved decision, not a backlog item.
- **The store-bound HTML modal files (AdminReset, ManageItems, ManageVendors, OrderHistory, ReorderPickPath, StorageAreas, HowToUse, RecalibrateVendor, VendorCadenceAudit, Styles) plus the 8 `.gs` files (MOGApi + Core + the 6 domain modules from the 2026-06-19 split) are all `apps-script/` peers.** No subdirectory structure inside `apps-script/` — they share one global scope at runtime. Clasp's default file picker handles this fine.
- **`apps-script/.clasp-targets.json` is committed to git** with real Script IDs. Script IDs are project identifiers, not secrets — pushing still requires OAuth (`clasp login`) on the user's side. Committing makes the deploy workflow portable to a fresh machine.
- **The `STORE_REGISTRY` build-injection marker** in root `index.html` is the line containing `// __STORE_REGISTRY__ build-injected`. Exactly one such line; `build.py` fails loud otherwise.
- **rpr's `appsscript.json` was chosen as canonical** during 2026-05-24 reconciliation because it explicitly declared OAuth scopes (the other 5 stores had implicit/auto-detected scopes). The canonical manifest is now applied to all 9 targets.
- **KM editor gate is VALIDATE-FIRST (2026-06-24) — the gate is the only door.** `mgeStartGate_` (`EditorShell.html`) no longer trusts a stored token optimistically: it validates it server-side via the new client-callable **`editorPing(token)`** (`Editor.gs`, an auth-layer fn that deliberately bypasses the `webedit_call` allowlist) before running any tool. **Why this matters:** session tokens live in `CacheService.getScriptCache()` (`putEditorToken_`), which Google **flushes on a new deployment version** — so a `--redeploy` silently invalidates every active token. The old optimistic gate then ran the tool with a dead token and bricked on the first RPC (`SESSION_EXPIRED`) with no recovery. Now a flushed/expired/missing token routes to the PIN, and **init re-runs on every successful auth** (`mgeRunCb_`, both initial and mid-session reauth) so a reprompt reloads the tool cleanly instead of leaving a blank page. Also kills the cold-deep-link brick. **Behavioral trade-off (accepted):** a mid-edit token expiry reloads the tool on re-PIN, dropping unsaved form input — the safe "recover to known-good data" default. Shared in `EditorShell`, so all 5 tools + home inherit it. **Live on all 9 + master (2026-06-24), not yet committed.**

---

## Recent significant changes

Most recent first. Trim entries older than ~5 sessions when this list gets unwieldy.

| Date | Session | Outcome |
|---|---|---|
| 2026-06-24 (later) | Fan-out + validate-first gate + per-tool mini-tours + MVS voice/chrome parity | **All canary-only Section B work + this session's fixes FANNED OUT to all 9 + master (`deploy.py --redeploy`, every target push+deploy OK); committed + pushed.** **(A) Validate-first gate** (`EditorShell` `mgeStartGate_` + new client-callable **`editorPing`** in `Editor.gs`): root cause of "redeploy bricks the editor" — session tokens live in `CacheService`, **flushed on a new deployment version**, so the old optimistic gate ran a tool with a dead token → `SESSION_EXPIRED` brick. Now a stored token is validated server-side before any tool runs (brief "checking" → tool, else → PIN), and init re-runs on **every** successful auth (`mgeRunCb_`) so a stale/expired token re-prompts + reloads cleanly. Shared → all 5 tools + home; kills the cold deep-link brick. Trade-off: mid-edit expiry reloads the tool on re-PIN (drops unsaved input). **(B) QOL #2 "Recently adjusted" pill** (`ManageItems`): client-side `mog_parAdjusted_<abbr>` (7-day, MOG_WEB-gated, self-pruning); `markParAdjusted_` fires in `doEdit` **only on a par change**; precedence over the flag in `buildFlagPill`/`buildFlagDetail`/`sortValue_`; `.pill-adjusted`. Table `.table-scroll` → `min-height:600px; max-height:clamp(600px,calc(100vh−220px),680px)` so the list card matches the open edit form. **(C) Per-tool mini-tours** on the 4 remaining tools (Vendors/Areas/Shelf-to-Sheet/History) — info mode, `<TOOL>_TOUR_STEPS` + `start/replay/maybeAutoStart`, once/browser, `.tour-replay-wrap` in each `?` help (CSS centralized in EditorShell). **(D) Tour gate hint** (`EditorShell`): gated steps show `ⓘ Do this step to continue` + relabel the button "Done with this step" (was a silently-disabled Next). **(E) Voice rewrite** of all 5 mini-tours + the home help popup to MVS/MPS voice (imperative titles, action-first bodies, no "~20 sec" meta, ES informal tú). **(F) Chrome parity:** `?` moved out of the band → prominent filled-accent circle next to the breadcrumb title (`.mge-crumb-help`, MVS `.vh-help`, held on `MGE_HELPBTN`); EN/ES stays in the band; `mgeMountWebChrome_` relocates only langs. Faded buttons fixed — `.mge-tour-btn.primary` `var(--accent)` (undefined on tools → navy) → `var(--web-accent, var(--accent, #1a1a2e))`. Home help-popup header colored with the accent. **(G) Skills/docs:** `mog-editor-web-reskin` **Rule 0 "port faithfully from MVS/MPS"** + Tours & voice section + centralized-chrome correction; `mog-deploy-workflow` "never `--redeploy` to iterate"; `MOG_CurrentState` validate-first-gate architecture note; NEW memory `feedback_editor_iterate_on_dev`. **Process: pull + confirm clean `git status` before deploying** (a stale checkout was deployed mid-session → the "super small table" rollback). **Next: auto-fire vs replay-only for the 4 new mini-tours; extend the "B" layout to the other tools; optionally globalize the MVS-fidelity lessons.** |
| 2026-06-24 | First-run setup wizard + guided tours + Manage Items QOL | **Phase A SHIPPED (committed `8aa7f6e`, pushed, fanned out all 9 + master); Phase B + Manage Items QOL CANARY ONLY (rpfrf editor / `_template` wizard), UNCOMMITTED.** **(A) `?page=setup` first-run wizard** (`Setup.html`, `Editor.gs` `renderStoreSetupWeb_` + `commitStoreSetup`): identity-only, one-shot token-less guard (refuses once `MOG_API_PIN` set; mints a token on save), writes the same props as `setupMobileApi` **minus master PIN**. MVS-style: device gate → EN/ES splash → **Concept + FOH/BOH dropdowns + City (only free text)** → auto store **name** + **code** (`CONCEPT + first-2-city-letters + BOH/FOH`, e.g. `TNYROBOH`, cap 10); concept dropdown **re-themes live**. 3 decisions locked (open-when-unconfigured; master PIN out; keep `setupMobileApi` fallback). **(B) `doGet` inversion** (`MOGApi.gs`): plain `/exec` → editor (configured) / wizard (unconfigured); `?page=editor` still works; JSON health → `?page=api`; PWA POST-only so unaffected (verified). **(C) `CONCEPT_THEMES`** (`Dashboard.gs`): +ĂN `#3C1124` (Night Market, brand kit) +Lei'd `#b51579`. **(D) Shared tour engine** promoted to `EditorShell` (`mgeStartTour_`, `info` + `gated` modes); **home flow-tour** (Vendors→Items→Areas→Shelf to Sheet, tile-anchored); **gated setup tour**. **(E) Manage Items:** mini-tour (MPS-voice copy, `?`-popup replay, once/browser); **prereq warnings** (amber no-vendor block / accent no-area tip); **natural-scroll layout** (unlocked dialog `html,body` lock via `html.mge-web`) + **persistent title/subtitle**; **live per-delivery-day par preview** — `ceil(par × dayMultiplier)` (round UP to whole, operator-confirmed; e.g. 3.25×3→10), mults from `getVendorTableData`. **(F) "Clear Config (reset for setup)"** menu (`MOGApi.gs`+`Core.gs`) — un-configures + clears concept to reuse the template for wizard testing. **Bug fixed:** `MIRPC` web proxy is an explicit allowlist — missing `getVendorTableData` threw mid-init, items stopped loading (added it + moved fetch after `loadAllItems`). NEW memory `feedback_canary_include_exec_link`. **Next: QOL #2 client-side "Recently adjusted" Par-Review flag; fan out Phase B + push.** Open: cold deep-link brick (`?page=items` un-authed → bricks; use bare `/exec` home link); per-tool mini-tours for the other 4 tools. |
| 2026-06-22 (later) | Editor audit + fan-out + TNY gold + Shelf-to-Sheet rename + grouped home + guided tour | **SHIPPED to all 9 + master; committed + pushed `bdb44c7` (carries the 4 base commits).** **(1) Audit clean** (`appsscript-codebase-audit` + dead-code check) — no dual-host dead code, no redundant init RPCs (prebaked template vars), cleanups left no residue. **The 5 Sheet-side candidates → KEEP all** (`showAdminResetSidebar`/`goToOrderEntry`/3× `toggle*Visibility`): not menu-wired or code-called, but `showAdminResetSidebar` is the sole entry to live Admin Reset → they're **dashboard-drawing-button-bound** (grep-invisible); confirm via "Assign script", don't delete. Cosmetic: EditorHome `:root` token dedup (drop `--font`/`--shadow`/`--muted`/`--faint`; EditorShell owns them; visual no-op). **(2) Fanned out** the parked Phase-1 + 06-22 polish to all 9 + master (`deploy.py --redeploy`, no `--target`) — first time the editor is on every store's `/exec`. **(3) TNY gold-forward everywhere** (web band + dashboard banner + recap email): `CONCEPT_THEMES` `accent` split into `accent` (band fill → gold `#D4A574`) + new **`ink`** (tiles/strip/email vendor headers+qty → charcoal `#1a1a1a`); `dashTheme_()` normalizes `ink: t.ink||t.accent` → **RP/default byte-unchanged**; `Dashboard.gs` tiles/strip/vendor-CF + `MOGApi.gs` email `headInk` use `ink` (+ luminance-derived `bandSub`); removed the web-only `webAccent` override (base is gold now). **(4) Info-notes removed** (markup in 5 tools + `.web-note` CSS); **breadcrumb left-aligned** (`.mge-crumb margin:0`). **(5) Renamed "Reorder Pick Path" → "Shelf to Sheet" / "Estante a Hoja"** everywhere shown (home tile, `Core.gs` menu, `Dashboard.gs` tile, `PickPath.gs` dialog, tool title/breadcrumb/help/Save→"Save", ManageItems+StorageAreas cross-refs, HowToUse); **left** code ids + AdminReset "Pick Path DB/List" data labels. **(6) Home grouped → 3 sections** (Catalog / Order Sheet Layout / Records) + **first-run guided tour** (MVS/MPS cutout-spotlight coach-marks, bilingual, once/browser via `localStorage`, lang from gate, replayable from help popup; engine in `EditorHome.html`, info-mode). Studied `Master-Visual-Schedule/MvsApp_Help.html` for the engine. **Manual: Rebuild Home Dashboard per store** (gold banner on 2 TNY, Shelf-to-Sheet tile on all). NEW memory `project_editor_webonly_direction`. **Next: build the first-time-setup wizard** (design + 3 open decisions in the handoff). |
| 2026-06-22 | KM editor web polish — remaining tools + shared shell + consistency | **Canary rpfrf `/dev` ONLY; Sebastian confirmed iteratively. NOT committed/pushed/fanned out.** Finished the per-tool web re-skins (mockup-approved first): **Manage Vendors** (920px, list + Add/Import/Remove cards), **Reorder Pick Path** (780px, **Treatment-B** area headers = soft-accent tint + edge bar, **Unassigned stays amber**, vendor-picker card, sticky Save footer; `setLang` clobber fixed), **Order History** (read-only, 1080px, 3 tabs → **pill bar**, filter → **toolbar card**, Recent → **vendor-card grid** via `.hist-date-group` CSS grid; its `setLang` never clobbered). **Then made the editor genuinely consistent (the bulk of the session):** **(1) Centralized header band into `EditorShell`** (was per-tool, the size-drift cause) — **brand horizontal logo → location → "Master Ordering Guide"** (horizontals from `heh-brand-kit/assets/logos`, `currentColor`-white, per `data-concept`); EN/ES **segmented control** + "?" **26px circle**, **fully owned via `!important`** so host base styles can't leak; `--shell-width` (per-page) drives band + breadcrumb width so the header **aligns with content**; each tool + **Home** call `mgeMountWebChrome_()`. Reference: **MPS/MVS shells**. **(2) Modular accent** — `mgeApplyConcept_` derives all four `--web-accent*` from `THEME.accent` via pure-JS `mgeMix_` (no `color-mix`); removed per-tool wiring; **fixes latent Teas'n You gold** (dark/soft were RP-teal-hardcoded). **(3) `.web-note`** shared info-note — one-line bilingual accent callout, now on **all 5 tools**. **(4) Consistency sweep** (`appsscript-ui-consistency-audit`, web layer only) — web tokens **defined once in EditorShell `:root`**, 5 per-tool copies removed; **fixed `ManageItems` referencing undefined `var(--r)`/`var(--faint)`** (corners/faint text broke on that page); tokenized stray radii (PickPath `5px`→`--r-sm`, OrderHistory tab `10px`/`7px`); normalized PickPath input `15px`→`14px`. Scanner's big raw-hex lists = **Sheet-mode base CSS** (out of scope, left raw). **Bugs fixed on `/dev`:** all-4-logos-showing (svg `display:block` beat `.mge-hl{display:none}` on specificity → scoped `!important`); location not shortened (`mgeShortLoc_` strips the concept prefix from `"Roll Play Founders FOH"` → "Founders FOH"); Home had no band. **All `body.mge-web`-scoped → Sheet dialogs byte-identical.** NEW memory `feedback_shared_shell_owns_chrome`. **Next: code audit (dead code + inefficiencies); then guided tour, fan out, `git push`.** |
| 2026-06-21 (polish) | KM editor web polish — Manage Items + Storage Areas | **Canary rpfrf `/dev` only (`deploy.py --redeploy --target rpfrf`); Sebastian confirmed both. NOT committed/pushed/fanned out at writing.** Bespoke per-tool web re-skin of the KM editor — all `body.mge-web`-gated, so the in-Sheet dialogs stay byte-identical. **Unblocker (cost the session):** every modal's `setLang` did `document.body.className='lang-'+lang`, wiping the `mge-web` class the re-skin is gated on → CSS *delivered* but never *activated* (looked like the old dialog). Fixed to toggle only the lang class — **do this FIRST on every remaining tool.** **Manage Items** (`ManageItems.html`): concept-accent band (lang/help relocated in) + centered 1500px master-detail + Inter + cards/`--shadow`; On Sheet + Par Review **sortable** (`sortValue_` ranks derived states); tabs → `Item details \| + Add item` toggle above the sidebar; View All + Inactive tabs → left **filter chips** (All active / Unassigned / Inactive; "All active" excludes inactive); client-side **New** badge (~7-day per-store first-seen); **in-place add/edit/delete** (no reload — auto-center + flash the row, blank sidebar after add, null-safe delete); `Items.gs` `clearDataValidations()` on the A:G block in `commitUpsertItem` fixes the "cell B43 violates data validation" add error (`insertRowAfter` inherited a stray NAME-column rule). **Storage Areas** (`StorageAreas.html`): accent band + centered 760px + **always-visible Add-area row** + **sticky Save footer** + web-only intro; draft/Save model intact. **Skills:** NEW `mog-editor-web-reskin` (turnkey per-tool recipe); ENHANCED `mog-deploy-workflow` (`/dev` live-HEAD vs `/exec` CDN-cached snapshot; editor canary = rpfrf; "deployed ≠ rendering"). **Next:** Manage Vendors → Reorder Pick Path → Order History (read-only, lightest), same recipe + setLang fix first; then guided tour, fan out, `git push`. |
| 2026-06-21 | KM web editor — Phase 1 | **3 commits (`4b5c353`, `fe47a12`, `1a3a799`), CANARY rpfrf ONLY — NOT pushed to GitHub, NOT fanned out. Sebastian confirmed all 5 tools work.** Built the parked KM-editor: a **computer-only, PIN-gated web app** at the existing `/exec` URL via **`doGet?page=…`** routing (`MOGApi.gs` — default JSON path byte-identical; PWA `doPost` untouched, phones unaffected). **5 live tools**: Manage Items, Manage Vendors, Storage Areas, Reorder Pick Path, Order History (read-only). **Admin Reset left Sheet-only** (destructive; not exposed to KMs — tile removed). **(Architecture)** **Dual-host modals**: each modal (`ManageItems`/`ManageVendors`/`StorageAreas`/`ReorderPickPath`/`OrderHistory`.html) serves BOTH the Sheet dialog and the web page via a `MOG_WEB` flag (injected `webBootJson`); Sheet launchers (`Items.gs`/`Vendors.gs`/`PickPath.gs`×2/`History.gs`) inject `web:false` → **parity by construction**. **New `Editor.gs`**: `renderEditorHome_` + per-tool `render<Tool>Web_`; `editorAuth(pin)` token-backed PIN reusing the existing `checkPin_`+lockout; **one allowlisted dispatcher `webedit_call(token,fn,args)`** every web RPC routes through (the `webeditDispatch_` switch IS the allowlist). **New `EditorShell.html`** (shared): concept-themed login (real brand logo extracted byte-exact from `template/index.html`, `dashTheme_()` accent), device gate, EN/ES toggle, breadcrumb chrome. **New `EditorHome.html`**: MVS-faithful dashboard (Inter, accent header + pill EN/ES, 880px container, fixed 3-col tiles, `--shadow`) + "Have a question?" help CTA + bilingual help popup. Client `MIRPC()` shim per modal (Sheet→`google.script.run`, web→token proxy). **Gotchas hit:** `google.script.run` can't call `_`-suffixed fns; **blank-modal regression** — `.mge-overlay{display:flex}` overrode the UA `[hidden]` rule (home masked it via its own `[hidden]!important`), fixed with `.mge-overlay[hidden]/.mge-crumb[hidden]{display:none!important}` in EditorShell; token shared cross-page via `localStorage`; `target=_top` links + injected `BASE_URL` (`ScriptApp.getService().getUrl()`); modal height models differ (Items+History `100%`+`mge-web` flex, Vendors+Areas `100vh` flex, PickPath none). **Candidate #1 DONE.** **Next: POLISH each tool's per-page look, then guided tour, then fan out + `git push`.** |
| 2026-06-19 (later) | God-object split (#4) + new-day dedup (#5) | **3 commits, all deployed all 9 + master via `deploy.py --redeploy`; canary rpfrf smoke-tested (menu, all modals, dashboard rebuild, reset, order history — Sebastian confirmed). Candidate #1 + #5 now DONE.** **(A) Split `OrderGuideScript.gs` (5,497 lines) → `Core.gs` + 6 domain modules** (`38382db`): `Vendors.gs`/`Items.gs`/`PickPath.gs`/`ResetLog.gs`/`History.gs`/`Dashboard.gs`; `Core.gs` keeps all global constants + generic helpers + menu/`onOpen`/`onEdit`/triggers + the order-cycle date helpers. **Pure code-motion** via a deterministic slicer (scratchpad) that partitions by top-level declaration, routes each chunk by name, and **verified byte-exact reassembly + 144/144 declaration parity** before writing. `node --check` clean on all 7; **zero duplicate symbols across all 8 `.gs`** (144 + 74 MOGApi = 218). Load-order is a non-issue because **all top-level constants stay co-located in Core** (the only top-level cross-refs live inside the constants block) — no `filePushOrder`/`deploy.py` change; the existing 2-file MOGApi split already proved multi-file global-scope sharing. Renamed the file (clasp `push -f` handles rename+delete atomically). **(B) Dedup new-day detection** (`a771767`): two canonical `Core.gs` helpers — `getActiveOrderDate_()` → `{date,dateStr,dayOfWeek}` (AE9→AE2→now) and `getResetStaleness_()` → `{today,lastReset,isStale}`; rewired 4 sites (`api_getResetStatus_` delegates, MOGApi's `getActiveOrderDate_` removed → Core, `dailyResetOnOpen_` uses `.isStale`, `getLogOrderDate_` returns `.dateStr`). **TZ standardized on `getSpreadsheetTimeZone()`** (all stores US/Eastern; no-op in practice). Net −8 lines. **(C) Doc/skill/comment ref updates** (`15ec141`): repointed `CLAUDE.md`/`README.md`/`apps-script/README.md` inventories, the "all `.gs` peers" CurrentState note, the 2 pattern skills (`mog-rpc-consolidation`, `mog-apps-script-caching` — also swapped a stale `commitAreaListMutation_` example for `commitStorageAreasDraft`), and 8 inline `.gs` comment pointers off the deleted file (synced push-only). `route.py` unchanged (generic `.gs` branch already routes the new files); 6 provenance banners kept. **Carry-forward:** dead-code side-task (Sebastian's Sheet-button check) still open; **next = PARKED KM-editor feature** (computer-only + PIN-gated; progressive Option A — `doGet?page=…`). |
| 2026-06-19 | Reset-fix + brand logos + audit & cleanup | **4 commits pushed; backend cleanup fanned out all 9 + master (canary rpfrf).** **(A) PWA auto-reset stranding fixed** (`cdc13f2`, CACHE v16→v17, PWA-only): `commitReset` now uses a 45s timeout (new optional `api()` `timeoutMs`; default 15s unchanged) + `runStaleReset_` **re-polls `getResetStatus` on failure** and enters the app via new `finishResetAndEnterApp_()` when the server completed despite a client abort — KMs no longer stranded on the reset screen. **(B) Brand SVG logos on PIN/splash** (`e805147`+`dab9b51`, CACHE v17→v19, PWA-only): single-color Canva lockups recolored via `currentColor`, shown per `[data-theme]` in place of the clipboard square. Live: RP `rp-stacked` (basil `--teal-dark`), TNY `tny-horizontal` clean lockup (charcoal `--text`). Dormant/wired-ahead: ĂN `an-stacked` (house green `--teal`), Lei'd `leid-horizontal` (magenta) — rules only fire on `data-theme="an"/"leid"` (no store ships them yet). Removed dead `.rp-outer`/`.rp-inner` CSS. SVGs from `heh-brand-kit/assets/logos/`. **(C) Two-layer audit + quick-win cleanup** (`c9023ab`, CACHE v19→v20): audit (`appsscript-codebase-audit` + `mog-pwa-audit`) verdict **clean**. Removed 7 dead backend fns (181 lines pure deletion; `OrderGuideScript.gs` 5669→5497, `MOGApi.gs` 2149→2141: `commitUpdateVendorMults`/`commitUpdateVendorCutoff` superseded by `commitUpdateVendorMultsAndCutoff`, empty `menuHeader_`, orphaned `reestablishVendorTemplateMenu_`, `hasLogEntryForDate_`, `getItemsByVendor`, `generateReference_`) via `deploy.py --redeploy`. PWA: added missing `recap` key to `T.titles`, `back-count-btn`→`reopenCountFromContext_()`, dropped dead `result.cancelled` branches + migration `console.log`. **Carry-forward:** VERIFY-then-delete `showAdminResetSidebar`/`goToOrderEntry`/3× `toggle*Visibility` (possible dashboard buttons); #4 god-object split + #5 new-day dedup (walkthrough first); PARKED KM-editor feature (locked: computer-only + PIN-gated; progressive Option A — serve modals via `doGet?page=…`). |
| 2026-06-08 | Vendor Import feature | **All to all 9 + master via `python deploy.py` (push-only, NO `--redeploy`); canary rprfo smoke-tested first; committed `da80844` + pushed.** New **"⬆ Import Vendor"** collapsible panel in `ManageVendors.html` (between Add and Remove). Flow: download a 2-col CSV template (`Item Name,Pack Size` — built client-side as a `Blob`, **no server roundtrip / no new OAuth scope**), fill on a computer, enter vendor name, check delivery days (mults derived at submit via existing `computeMultsFromDelivery`; `recalcImportSummary` shows a live "Delivers: …", no editable mult grid), optional cutoff, upload the CSV. File read client-side via `FileReader`→text (`onImportFilePicked`) with a live "**N items found**" preview (`countImportItems_` mirrors the server skip rules) gating the Import button — **desktop-only by design** (Sebastian's call; no phone file-picker plumbing). NEW server fn **`commitImportVendor({name, mults, cutoff, csvText})`** in `OrderGuideScript.gs` (after `commitAddVendor`): `Utilities.parseCsv` (skips blank/`#`-comment/`Item Name`/`Name` header rows, dedupes by lowercased name → `skipped[]`), **reuses `commitAddVendor`** for the vendor (SETUP block + `VENDOR_TEMPLATE` clone in one place), then **bulk-inserts items into `MASTER_ITEMS`** via `insertRowsAfter(n)` + batched `setValues` for A:G / col O / L:N (NOT per-item like `commitUpsertItem` — avoids hundreds of I/O calls + exec-time limit). Items land **par BLANK + no storage area** (skeleton catalog — stay OFF the vendor's ordering tab until assigned an area in Manage Items), ACTIVE + USE_MULT `true`, eligible-vendors = just the imported vendor. Returns `{ok, vendor, itemsAdded, skipped}`. **CSV not xlsx** (Apps Script can't natively parse xlsx; CSV = zero deps/scopes). 6 EN/ES keys added, parity **34/34 PASS**; `.gs` + modal JS parse clean. |
| 2026-06-04 (later) | Recap-email rebrand (Phase 2) + modal sweep fan-out | **All to all 9 + master; canary rprfo smoke-tested.** **(A)** Closed the modal-sweep gate — `python deploy.py` (push-only) fanned the shared-`Styles.html` + sizing-tier sweep out to all 9 + master (the 2026-06-04 canary row above is now live everywhere). **(B) Daily recap email rebranded** (`MOGApi.gs sendRecapEmail_`, `--redeploy`): warm palette → **per-concept theming via `dashTheme_()`** (`MOG_CONCEPT`: RP teal-dark `#2d8c6b`+white / TNY charcoal+gold `#D4A574` / unset→navy — same palette as the Sheet dashboard; `dashTheme_()` lives in `OrderGuideScript.gs` but is callable from `MOGApi.gs`, `typeof`-guarded). Header band: **location bold on top**, "Suggested daily order" below, faint `date · vendors · items`. **"Suggested" wording** (caption + `Qty`→"Suggested order"). Each line reads **`Item Name × 3 (Pack)`** — name body-ink, **× qty bold in concept accent**, pack muted; old 4-col grid replaced. **On Hand isolated** right, faint (12px `#aab0b6`). Email-safe: literal hexes, single-cell `<table>` band; plain-text body aligned too. **(C) NEW editor-only `test_recapEmailToSelf()`** — previews the recap to whoever runs it (`Session.getActiveUser()`), bypassing recipient list / dedupe flag / On-Hand clear; not menu-wired. **Zero new OAuth scopes** (`userinfo.email`/`script.send_mail`/`spreadsheets` already declared) → fan-out re-prompts nobody; do NOT touch `appsscript.json` scopes. MOGApi.gs parses clean (node --check). |
| 2026-06-04 | Modal design-system unification (shared Styles + sizing tiers) | **rprfo CANARY ONLY (push-only); fan-out to the other 8 + master PENDING a live smoke-test.** Cohesion sweep across all 9 modals (`appsscript-ui-consistency-audit`). **NEW `apps-script/Styles.html`** = single source of truth: `:root` design tokens + universal chrome (`*` reset, `.lang-*`, `[?]` `.help-*` overlay) that was copy-pasted per modal (~200 dup lines removed), pulled via `<?!= include('Styles'); ?>`. New `include()` helper in `OrderGuideScript.gs`; 3 flat modals (HowToUse/AdminReset/OrderHistory) converted to `createTemplateFromFile().evaluate()`. All brand/semantic hexes → `var(--*)` (`--brand` navy header uniform, `--brand-green`, `--accent`, `--success`, `--danger`, `--warn`; `--r-control 6`/`--r-card 8`); neutral greys left raw (noted optional). **AdminReset `#7a1a1a` split**: header→`--brand`, destructive affordances→`--danger` (VERIFY-1/2 approved). **Two-tier sizing** constants `MODAL_SM 720×680` / `MODAL_LG 1400×900`; every `showModalDialog` retrofitted — SMALL: Vendors(↑from 620×600)/Storage/AdminReset/PickPath/Recalibrate/Cadence; LARGE: Items/OrderHistory(matches Items)/HowToUse. Fixed `[?]` off-center on Items+History (`.lang-group{align-items:center}` shared) + invisible `[?]` on PickPath (`.lang-bar{color:#fff}`). OrderHistory readability bump (base 14/table 13, auto-layout tables so no wrap); print summary header tokenized. Verified: zero raw brand hexes, 1 include/modal, 6 `[?]` intact, `<style>` braces balanced, EN/ES parity 9/9. **Open gate: smoke-test rprfo → `python deploy.py` fan-out. Then Phase 2 recap-email rebrand (`MOGApi.gs`, `--redeploy`, email can't use CSS vars).** |
| 2026-06-01 (cleanup) | Backend cleanup + per-modal [?] help + guide de-stale | **All to all 9 + master; canary rprfo on both deploys.** Grounded in a read-only `appsscript-codebase-audit`. **(A) Backend cleanup** (`deploy.py --redeploy`): deleted dead `migrateVendorTabs`+`brandAndStripVendorTab_`, `getOrderSummary`, `getOrderHistoryVendorList` (~230 lines; `OrderGuideScript.gs` 5783→~5550); decluttered Mobile API submenu — removed `Migrate Item Vendors`/`Audit Vendor Tab Structure`/`Re-establish Vendor Template` entries (**functions kept, editor-runnable**); trimmed dead `dayOfWeek`/`vendorMults` args off `vendorOnHandSnapshot_` (call+def); fixed 3 stale comments. `purgeInactiveFromPickPath` **kept** (intentional editor-run tool). **(B) Per-modal [?] help** (push-only): round [?] in each modal header → inline bilingual help overlay (`position:fixed` backdrop, ✕/tap-outside/Esc), `currentColor`-adaptive, paired `.en/.es` spans (no new glossary keys, parity intact). Identical CSS/JS block across **6 modals** — StorageAreas canary-first, then ManageVendors/ManageItems/AdminReset/ReorderPickPath/OrderHistory. Content verified vs LIVE modals (Items=row→Edit This Item; Pick Path=▲▼/⇆/+; Areas=▲▼+Save Areas). **(C) HowToUse de-stale**: Items Edit-tab→row+Edit This Item, Areas drag→▲▼+Save Areas, Order History 📋Copy→🖨Print (+dropped Avg On Hand), all Pick Path drag→▲▼/⇆/+. **Deferred:** `OrderGuideScript.gs` 7-file split (Core/Vendors/Items/PickPath/ResetLog/History/Dashboard — walkthrough first) + new-day-detection consolidation (~5 sites); optional guide adds (PWA pointer, multi-vendor switch, Recalibrate, recap-email). All `.gs`+modal scripts parse clean (vm.Script); EN/ES parity PASS 9/9. |
| 2026-06-01 (later) | Ordering-flow breadcrumb + vendor topbar | **Two PWA commits, both pushed to `main` (GitHub Pages); no clasp. CACHE v14→v15→v16.** History breadcrumb's open gate **closed** (Sebastian confirmed it works live on rprfo). **(A) Ordering breadcrumb** (`3631927`): extended the History drill-down breadcrumb to the ordering flow — count `Today's orders › <vendor>`, review `Today's orders › <vendor> › Review order`. Reused generic `setBreadcrumb_`; new `setCountBreadcrumb_(vendor)` (shared by drill-in + return-from-review, no drift); `showView` breadcrumb keep-list widened to `count`/`review`. Ancestor crumbs tappable (root → today, vendor → count); count root crumb routes through `goBack` so the mid-count save/discard guard fires. **Back button on review now steps review→count** (was review→today), matching the crumb. **Correctness catch (walkthrough):** `onReviewClick`'s flush is no-refetch and *deletes the in-memory vendor cache* (`delete state.cache.vendorItems[vendor]`) + `clearDraft`s, so routing review→count via `openCount` would re-fetch a stale localStorage snapshot and revert the just-entered counts → added `reopenCountFromContext_()` that re-renders count from in-memory `ctx.items` (no fetch); both the review vendor-crumb and back button use it. Initially also deduped the topbar (mirroring History), then **(B)** reverted that half (`74529c0`) per Sebastian's live review: count/review **topbar shows the vendor name** again for clarity, breadcrumb stays underneath; `setTitle('count'/'review', ctx.vendor)` restored, `titles.count`/`titles.review` kept as empty-vendor fallback, redundant review `section-label` stays removed (resolves "Review order shows twice"). Verified: inline `<script>` blocks parse clean (vm.Script), `build.py` 8 dirs + hub registry unchanged. |
| 2026-06-01 | Housekeeping batch + PWA History breadcrumb | **Two commits, both pushed.** **(A) Backend housekeeping** (`4d5a05e`, `deploy.py --redeploy` to all 9 + master, canary rprfo smoke-tested): `commitUpsertItem` (ADD + EDIT) now returns `areaError` on a failed inline `commitPickPathAreaAssignment` — both `ManageItems.html` partial-success toasts (`addedNoArea`/`savedFail`) append `(reason)`; item still saves `ok:true`, the swallow was only of the *reason*. Retired `api_getHistory_` from `MOGApi.gs` (dead — the "daily-recap uses it" comment was false; recap uses `buildRecapSections_`, PWA migrated off `getHistory` 2026-05-28) + dispatch `case 'getHistory'` + `test_getHistory`; `getOrderHistory` itself kept. Reconciled the Rhino-ES5 invariant: `CLAUDE.md` invariant #4 + pitfall #5 + the `MOG_CurrentState.md` mirror now say modals render browser-side (IFRAME sandbox, ES6 already in prod) and `.gs` is V8; **global `rhino-safe-html` left untouched** (cross-repo infra — flagged optional follow-up). Purged "parallelize `deploy.py`" from the candidate list + the @-imported 2026-05-31 carries; added a decision record to architecture notes + a "Dropped (do not re-add)" note. **(B) PWA History breadcrumb** (`a0ca5b6`, `build.py` + `git push`, GitHub Pages, CACHE v13→v14): breadcrumb strip under the topbar on `view-history-vendors` + `view-history-detail` — `Order history › <date> › <vendor>`, ancestor crumbs tappable to that level, current static, hidden on the dates root + non-History views. New `setBreadcrumb_(segments)` (per-segment pre-prepared HTML — date crumb raw because `formatHistoryDate` carries `&middot;`, vendor `escapeHtml`'d) + `goToHistoryRoot_`; wired into `openHistoryDateCard`/`openHistoryDetail`/`goBack` + cleared in `showView`. Removed the redundant date `section-label` (vendors) + trimmed detail header to the order timestamp + dropped the topbar date-title override (clean-dedup variant Sebastian chose). Brand-neutral gray chrome. Verified: embedded `<script>` blocks parse clean (vm.Script), `build.py` no placeholder errors, 8 dirs regenerated, hub registry unchanged. **Smoke-test on rprfo CONFIRMED working (2026-06-01 later session).** |
| 2026-05-31 (later) | Hub 3-level nav + per-concept tile theming | **Hub-only + tooling change; one suggested commit, `git push` (no clasp).** (A) **3-level hub nav** (`index.html`): Concept → **Site** (Rosslyn/Tysons/Founders) → **Area** (BOH/FOH), was Concept → flat location list. Site/area **derived from the `location` string** (Option A — `parseLocation_` splits the trailing all-caps token; new `buildSiteGroups_`, `onSiteTap_`, `screen-area` markup + mgr banner, `areaBackTarget` 3-level back-nav). **No `stores.json` schema change** → `build.py`/`mog-add-store` untouched. Collapse rules: single-site concept (TNY) skips the site screen → area picker (back→concept); single-area site skips area screen → store. Chosen over explicit `site`/`area` fields because the `"<Site> BOH/FOH"` naming is 100% regular and `location` is already the downstream display string (explicit fields would duplicate + drift). (B) **Per-concept tile theming**: `CONCEPT_VISUALS` gained `tint`/`tintDark`/`tintLight` (RP basil `#53D3A5`/`#2d8c6b`/`#e4f7f0`, TNY gold `#D4A574`/`#8a6d3b`/`#f7efe2`); concept chips tinted inline per-card, active concept tint pushed to `#app` as `--concept-dark`/`--concept-light` (`setActiveConceptTheme_`) so site/area chips + back-btn inherit it, cleared to HEH-red on the picker. (C) **TNY curly apostrophe** `Teas'n You`→`Teas’n You` (U+2019) synced across `stores.json` + hub `CONCEPT_VISUALS` key + `build.py` `CONCEPT_TO_THEME` key (else theme lookup falls back); verified `tnyt`/`tnytf` still resolve `data-theme="teasnyou"` post-build. (D) **`audit_modals.py` widened** (`mog-modal-ux-sweep`): stale hardcoded 5-modal list → auto-discovery (same `looks_save_capable` heuristic as the global copy, scoped to `apps-script/`); now covers all 6 save-capable modals incl. `RecalibrateVendor.html`, excludes the 3 read-only, never goes stale (6/6 green). (E) Latent fix: `toggleHubLang_` now re-renders the picker (the "N locations" meta is built text, not bilingual spans). (F) `mog-session-handoff/SKILL.md` gained a "run before the feature commit" ordering rule. Hub `sw.js` CACHE **v6→v7**; `build.py` ran but the 8 `<slug>/` dirs came out **byte-identical** (no redeploy). Verified by driving the hub in a local `http.server` preview (all 3 levels + collapse + theming + back-nav) + `node --check` + audit script. |
| 2026-05-31 | Visual-consistency sweep (Batch A/B + Manage Items) + basil brand fix | **Three commits, all live + deployed.** Read-only audit of Hub + PWA + Modals vs 3 yardsticks (internal cross-surface / Canva brand-kit / UI polish), then shipped the safe fixes with decision gates. **Batch A** (`41f2e8b`, PWA, `build.py`+push, CACHE v12→v13): new `--teal-border` token in `template/index.html` (`:root` + roll-play/teasnyou/leid theme scopes); 7 hardcoded HEH-teal literals (`.toast.success`, `.pill-done`, `.manager-banner`×2, `#b8dccd` borders on `.alldone-panel`/`.recipient-add-form`/`.vendor-card.status-reviewed`/`.recipient-toggle.on`, plus `#d8efe1`/`#1a6b2e`) → tokens so brand color re-themes per concept; merged duplicate `body` block. **Principle locked: primary/brand re-themes, red/amber semantic warnings stay constant.** **Batch B** (`3b48218`, modals via `deploy.py` push-only + hub via push, hub CACHE v5→v6): `HowToUse` header green `#1a3a2e`→navy `#1a1a2e` + page bg `#f4f5f7`; `ReorderPickPath` picker glyph `×`→`✕` + page bg; `AdminReset`/`ManageVendors` page bg unified to `#f4f5f7`; hub `index.html` meta theme-color teal→red `#fc0404` (match manifest). ManageItems navy base button LEFT navy (documented-intentional: navy=nav, green=commit). **Manage Items** (`7210e0d`, modals push-only): `.table-legend` enlarged (11→13px, swatches 13px, more padding) to fill dead space; `buildFlagPill` rewritten — reviewed-OK → gray "✓ No change" pill (`.pill-no`), `<5 orders`/no-history → softer gray "No data yet" pill (new `.pill-nodata`), inactive → `—`, real flags → amber/red ⚠; new `flagOk` EN/ES key, parity 103/103. **Brand fix (outside repo):** Roll Play primary corrected to **basil green `#53D3A5`** in `~/.claude/skills/heh-brand-kit/.../rp-design-tokens.md` (orange `#F73F06` demoted to accent); the PWA already used `#53D3A5` for RP so app + brand doc now agree. Canary rprfo on every deploy, then fanned out all 9 + master. |
| 2026-05-29 (evening) | #4 build → vendor-template H2 root-cause fix | **Shipped all 9 + master (push only); committed at session close.** Built #4 scaffolding (`reestablishVendorTemplate_`, fail-safe `commitAddVendor`, `brandAndStripVendorTab_`, `migrateVendorTabs`), then **pivoted after ground-truthing all 9 stores from fresh `.xlsx` exports**. Findings: vendor tabs are structurally **identical clones** — the `M3`/`I4`/`K4` audit "variants" were just Google's `__xludf.DUMMYFUNCTION("<formula>","<cached value>")` export wrapper (formula text identical, only the cached tail differs); `ORDER_ENTRY` canonical = `AD2` (override) / `AE3` (day), legacy `B4`/`D2` dead/empty. **The one real bug:** `VENDOR_TEMPLATE!H2` stale (`B4`/`D2`) on **6 of 7 templates incl. the master** — `updateVendorTabHeader2Formulas_` loops `getVendorList()` (live tabs only) and never touches the hidden template, so Add-Vendor clones inherited a 0 multiplier (nothing orderable). **Fix:** `vendorTabH2Formula_()` canonical helper; `updateVendorTabHeader2Formulas_` now ALSO repairs `VENDOR_TEMPLATE`; new non-destructive menu **Sync Vendor Multiplier Formulas** (no dashboard rebuild). `migrateVendorTabs`/`brandAndStripVendorTab_` **shelved** (cosmetic, unwired from menu). Sebastian ran Sync (+ Re-establish on rptfo/rpfrf) on every store, re-exported, **verified clean: templates present on all 9, all `H2` = `AD2`/`AE3`, zero stale tabs**. tnytf **Setup re-run** restored its script properties (lost in its 2026-05-26 script-project move; properties are project-scoped). |
| 2026-05-29 (latest) | Manage Vendors clarity + Dashboard branding + #4 scope | **Two features shipped to all 9 (deployed via clasp; commits PENDING — Sebastian owns the commit). (A) Manage Vendors multiplier→delivery clarity** (`ManageVendors.html`, bound-sidebar): per-cell "→ <day>" arrows under each active multiplier (Add+Edit; `updateMultFeeds_` folded into `styleMultInput` + `data-day` attr), a worked-example callout at the top of the Edit form (`multExample` key, EN/ES, Mon/Thu/Sat case), and a latent fix — `setLang` never re-rendered the vendor cards so the EN/ES toggle left card chrome in the build-time language → added `rerenderVendorCards_()`. Parity 26/26. **(B) Dashboard per-store branding** (`MOGApi.gs` + `OrderGuideScript.gs`, `--redeploy`): `buildHomeBanner_` was hardcoded "ORDERING GUIDE · ROSSLYN" (every rebuild stamped ROSSLYN) → now reads `MOG_LOCATION_NAME`; new `MOG_CONCEPT` property drives the accent via `CONCEPT_THEMES` + memoized `dashTheme_()` (RP teal-dark `#2d8c6b`/white, TNY charcoal `#1a1a1a`/gold `#D4A574`; unset→navy) on banner + tiles + reset strip; set via new `setStoreConcept()` + extended 6-step `setupMobileApi()`. Canary rprfo (RP) + tnyt (TNY) verified; **6 stores still need concept+rebuild**. **(C) #4 vendor-tab redesign scoped:** read-only `auditVendorTabStructure()` deployed all 9. Found **VENDOR_TEMPLATE DELETED on rprfo** (add-vendor *copies* it, doesn't recreate; falls back to fragile `ss.getSheets()[3]`); uploaded xlsx is STALE vs live (trust live template); order block I–L is human-facing only; N–T dead (Q–T = unreferenced dupe of A–D). **#2** assign-after-reset bug parked (model confirmed, couldn't reproduce). |
| 2026-05-29 | Vendor Cadence Audit + PWA decimal On Hand | **Two commits, both live.** (A) **Vendor Cadence Audit** (`9b4c027`, bound-sidebar, all 9): new entry at Ordering Guide → 📱 Mobile API → Audit Vendor Cadence. Read-only diagnostic — for each vendor, runs `inferDeliveryFromMults_` → `computeMultsFromDelivery_` (server-side twins of the existing client helpers) and flags 3 statuses: `canonical` (round-trips losslessly — picker promotion would be safe), `mismatch` (recompute differs from stored — recalibrate first), `everyday` (all 1s, every-day delivery — confirm vendor really delivers daily). Modal `VendorCadenceAudit.html` (NEW, 21 EN/ES keys, parity PASS): summary chips, legend, sorted table (mismatch → everyday → canonical) with cell-level diff outlines on mismatch rows. No save path, no schema change. Tool exists to clean the data before the deferred ManageVendors Edit-form disclosure ships. (B) **PWA decimal On Hand input** (`daf354f`, GitHub Pages, CACHE_VERSION v11→v12): 3 edits in `template/index.html` — `inputmode="numeric"` → `"decimal"` + `step="any"` on the count input; `parseInt` → `parseFloat` in `onStepperClick` and `onCountInputChange`. Server `api_saveOnHand_` already does `Number()` so decimals reach the Sheet cleanly. KMs can now enter `.4`, `1.5`, etc. (C) **Architectural walkthrough — ManageVendors edit-form redesign**: shipped no code but locked 3 decisions. Gate 1=(b) hide raw mults grid behind "Advanced" disclosure (deferred until audit findings are worked). Gate 2=(b) NO lead-day picker (operational convention: always 1-day lead, size pars accordingly). Gate 3=(c) defer schema migration. The "edit-form redesign" backlog item is now split — design done, ship gated. |
| 2026-05-28 | Five UX/perf/data-tool wins | **Multi-topic session; four commits + one pending. CACHE_VERSION v8→v9→v10→v11.** (A) **StorageAreas draft mode** (`ed49a62`, bound-sidebar): 4 granular RPCs + helper replaced by one `commitStorageAreasDraft(finalList)` atomic reconciler (delete/rename/add/reorder in one pass; map-based pick-DB remap fixes a latent name-swap bug). Drag-to-reorder retired → stacked ▲▼; Close + Save Areas split to opposite footer ends. (B) **PWA transition floor cut** (`6f9197f`): `playTransitionAnimation` MIN_DURATION_MS 1100→500 + outro 750→400 → ~1.85s→~900ms on every navigation. Pulse still engages for slow work. (C) **PWA fetch timeout** (`ba30b8e`): AbortController + 15s in `api()`; `isNetworkError_` recognizes `AbortError` → hung requests fall into the offline queue. Bundled `--r-sm: 6px` token completion. (D) **Chunked Order History** (`47a1966`): three-tier load (dates → vendors → items) via two new `CacheService`-wrapped server endpoints (`api_getHistoryDates_`, `api_getHistoryVendors_`). PWA stops calling `getHistory` (kept for internal daily-recap caller). `--redeploy` required. New PWA view `view-history-vendors`, local caches `state.cache.historyDates` + `historyVendors[date]`. (E) **Recalibrate Vendor Pars tool** (pending commit, deployed via `python deploy.py`): new modal at Ordering Guide → 📱 Mobile API → Recalibrate Vendor Pars. Three divisor framings + custom; **ceil-to-nearest-0.5** rounding bias (Sebastian's call); filters to items active for this vendor with `USE_MULT=true`. `getVendorRecalibrationBootstrap` + `commitVendorRecalibration` (atomic mults + filtered par division + `bumpServerMutationTs_`). 20 EN/ES keys, parity verified. Unblocks rpr's 1-day-par recalibration. |
| 2026-05-27 | Modal consistency check + PWA audit + fixes | **PWA-layer, `build.py` + git push, `CACHE_VERSION` v7→v8, commit `b7b8aeb`, NO clasp / NO `--redeploy`.** Full audit of `template/index.html` (6128 lines) + `sw.js`. One real bug + 4 cleanups: (1) **`uiIsInteractive_()` dead gate** — the background-refresh "don't repaint Today under an open modal/busy overlay" check looked for a nonexistent `#modal-overlay`/`.show`; real elements are `#modal-backdrop`/`#busy-overlay` toggling `.open`, so it always returned false (regression from a modal-id refactor). Fixed. (2) localized two `' vendors'` toasts (EN/ES). (3) removed leftover reset `console.log` (+ unused `result`). (4) `--text-1`→`--text` undefined-var typo on `.recipient-name`. (5) corrected stale SWR comment in `sw.js` (handler is network-first). Modals re-checked vs each other and **left as-is** — candidate nits would break a layout (AdminReset centered status) or shuffle consistency (ManageItems theme green). Built to all 8 store dirs + pushed; verify on live URL (canary rprfo) at v8. |
| 2026-05-27 | Modal Close/chrome sweep + ManageItems layout + OrderHistory revamp | **Store-facing, big — all bound-sidebar, no `--redeploy`.** (A) Chrome de-dup: removed self-added title (+ OrderHistory top ✕) doubling Google's `showModalDialog` chrome on OrderHistory & ManageVendors (Group A only); compact bottom-right footer Close normalized across ManageItems/OrderHistory/ManageVendors/StorageAreas. (B) ManageItems layout: `.shell` `100vh→100%` (Apps Script iframe fill bug — `100vh` under-resolves in the sandboxed dialog), right pane 390→300px + cell padding 12→7px + dropped per-col max-widths + `showManageItemsSidebar` width 1200→1400 so all 8 cols show, **table header split into its own non-scrolling table (`table-layout:fixed` + shared `<colgroup>`) so the scrollbar starts below the header**, styled 12px scrollbar + matching head gutter for alignment. (C) OrderHistory revamp (100% client-side — already reads public `getOrderHistory`/`getOrderHistoryBootstrap`): Recent → PWA-style date-grouped vendor **cards** + click-to-detail; Item History → per-item **accordion** (Item ID dropped, repeated date gone, On Hand kept); Vendor Summary → **collapsible** (closed on All Vendors, auto-open when filtered; Item ID→Case Pack; Avg On Hand dropped; totals = unique item count); Copy→**Print** (`window.print()` + `@media print`); card pop-out styling. Dead code/i18n pruned, EN/ES at parity. Canary **rprfo** (~8 iterations), then all 9 via `python deploy.py`. **Standing change: rprfo is the canary now, not rpr.** |
| 2026-05-27 | ManageItems multi-vendor items + modal declutter | **Store-facing, big.** Items can be orderable from multiple vendors (eligible list in `MASTER_ITEMS` col **O** — a NEW column; SKU@D kept since every vendor tab `XLOOKUP`s D for its hidden SKU display) + a one-tap "Active vendor" quick-switch on the View detail. `commitSwitchActiveVendor` moves the item to that vendor's tab; **par is shared per-item, the vendor day-multiplier does the rest** — verified math-safe against the real RP_ROSSLYN_FOH sheet formulas. New `parse`/`serialize`/`normalizeEligibleList_` helpers (reads self-heal — eligible defaults to active vendor pre-backfill), `commitUpsertItem` writes O on add+edit, `migrateItemVendorsColumn` one-time per-store backfill (menu: Mobile API → Migrate Item Vendors; optional sheet hygiene). Declutter (6 asks): removed Edit tab + vestigial selectors (RPC removed — `loadItemIntoEdit`→`getItemForEdit`), legend reworded (Possible under-/over-ordering, dropped Inactive), no-scroll table, Inactive/Unassigned tab count chips, green Add panel, removed the modal's self-added title+✕ that doubled Google's `showModalDialog` chrome. Both cache keys bumped (`getAllItemsForView` item shape gained `eligibleVendors`). Bound-sidebar push to all 9 (no `--redeploy`); canary rprfo (3 iterations). **CAVEAT:** rpr pars may not be 1-day pars — recalc before using the switch there. |
| 2026-05-26 | PWA fixes + ManageVendors picker + modal sweep + build.py guard | **Store-facing, backlog-clearing session.** (1) PWA new-day reset overlay fix — `handlePinSubmit` now lets the transition bar finish/fade before the `z70` "Detected new day…" overlay (was hidden behind the `z80` bar on the PIN-entry path). (2) Cached PIN removed — session-only now; boot wipes any legacy `mog_pin`; master-PIN path untouched. (PWA `8aa491a`, `CACHE_VERSION` v7.) (3) **ManageVendors edit-form delivery-day picker** (`c86d9d3`) — NO schema change: `computeMultsFromDelivery` (shared with add-form) + `inferDeliveryFromMults` (display-only seed) + `recalcInlineMults`; non-destructive until the KM toggles a day. Canary rpr → all 9. (4) Modal close-affordance sweep (`588b20d`) — OrderHistory `×`→`✕`, HowToUse `btn-close` green→grey (lone outlier; 4/5 already grey). (5) `build.py` hub-registry injection now idempotent — `[skip]` no-op instead of crash on unchanged stores.json (`0c11016`). Housekeeping confirmed: old repo decommissioned, all 8 trigger installs done. Noted: invariant #4 (Rhino/ES5) is inaccurate — modals run in the browser; new code still written ES5-safe. |
| 2026-05-26 | New-day auto-reset + guaranteed recap email | **Store-facing.** Recap email now a deduped side-effect of `commitLogAndReset` (`sendRecapIfUnsent_` helper, gated by `MOG_LAST_RECAP_SENT_DATE`) so every reset path emails once per cycle — closed the gap where the PWA stale-gate and `api_commitReset_` never emailed. PWA auto-fires the reset on first open of a new day (`proceedAfterAuth` → `autoRunStaleReset` → shared `runStaleReset_`), with a "Detected new day" overlay; manual button kept as fallback. New installable `onOpen` trigger `dailyResetOnOpen_` + `ensureDailyResetTrigger_` installer (wired into `buildHomeDashboard`) does the same on Sheet-open — installable because a simple trigger can't call MailApp. Latent-bug fix: `buildHomeDashboard` now preserves AE9 across the rebuild (was blanked by the rows1-50×cols1-35 clear → banner reded out + rebuild looked like a new day). `CACHE_VERSION` v5→v6. Canary on rpr (@17) smoke-tested (banner-preserve + recap-on-reset confirmed), then fanned out to all 9 + PWA pushed. Commit `04b7098`. **Outstanding manual step:** run "Rebuild Home Dashboard" per Sheet (8×, one auth prompt each) to install the open-trigger; PWA path already live without it. |
| 2026-05-26 (later session) | Skills expansion + full skills audit | No store-facing code. MOG skill set 4 → 7: added `mog-rpc-consolidation`, `mog-apps-script-caching`, `mog-modal-ux-sweep` (patterns re-derived every session). Then a 3-part audit: (visibility) `disable-model-invocation` on add-store, `user-invocable:false` on the 3 pattern skills; (determinism) 3 new stdlib scripts — `mog-deploy-workflow/scripts/route.py` (deploy router, guards pitfall #4a), `mog-add-store/scripts/validate.py` (Script-ID/exec-URL checks), `mog-modal-ux-sweep/scripts/audit_modals.py` (modal drift detector); (composability) canary/redeploy logic de-duplicated from 5 skills into route.py as the single source of truth. All scripts smoke-tested. Gotcha caught: audit_modals.py's first close-button signature false-flagged AdminReset/ManageItems (their markup differs) — recalibrated to `google.script.host.close()`. Commits `d1d72e0`, `ebce6b9`, `79785bc`; the two final scripts + 2 SKILL.md wirings still uncommitted. |
| 2026-05-27 | Audit punch list close-out + modal UX polish | 6 of 7 audit items shipped: #3 dashboard CacheService + count-items-by-vendor loop hoist; #5 fetchCurrentArea removal + getVendorTableData read merge + getSheet_ per-execution memoization; #4 commitAreaListMutation_ helper (option A — server-side dedup, not full RPC consolidation); #7 CACHE_VERSION audit closed clean at v5. Plus UX polish: ✓ + green-flash on all 5 modals' .status.ok; new Close buttons on StorageAreas + ManageVendors; StorageAreas Delete Area panel removed (inline 🗑 + window.confirm covers it); inline trash now red at rest; flex-column scroll layout fix on StorageAreas (gotcha: `.body > * { flex-shrink: 0 }` needed or .list-card's overflow:hidden clips rows). All 9 targets pushed + redeployed. |
| 2026-05-26 | ManageVendors save consolidation + Python deploy tool | Audit item #2 shipped: `commitUpdateVendorMultsAndCutoff` server fn + flat one-RPC client (was two chained RPCs with shared row lookup). Deployed to all 9 targets (bound-sidebar-only — no web-app redeploy needed). Tooling overhaul: built the PowerShell `deploy.ps1` + `discover-deployments.ps1` pair with `-Redeploy` support, populated all 8 `deploymentId` fields in `.clasp-targets.json`, migrated TNYTF to a new script project, then ported the whole tool to Python (`deploy.py` at repo root, parallel to `build.py`). Closed the gap where MOGApi.gs changes silently didn't reach the PWA's `/exec` URL — `python deploy.py --redeploy` handles push + version bump in one command. Cross-platform, zero deps. Docs updated: `apps-script/README.md`, `CLAUDE.md`, this file, deploy-workflow + add-store skills. |
| 2026-05-25 | Order History modal RPC consolidation | Apps Script modal perf audit produced 7-item ranked punch-list. Item #1 shipped: `getOrderHistoryBootstrap` server fn + rewired `OrderHistory.html` window.onload → 1 RPC instead of 2 on modal open, 1 fewer `LOG_ORDERS` read. Deployed to all 9 clasp targets. Side-note: Node + clasp + clasp login installed on `sebcn` machine for the first time. |
| 2026-05-24 | Repo consolidation + Claude Code scaffold | Master-Ordering-Guide repo merged into mog-mobile; `apps-script/` folder created; clasp deploy workflow set up; 2 new stores (rpfrf, rptfo) onboarded; CLAUDE.md + docs/ + 3 repo-specific skills written. Commits: `d95080f`, `bb68221`, plus this scaffold commit. |
